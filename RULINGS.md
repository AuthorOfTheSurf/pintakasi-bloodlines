# RULINGS

A ledger of WHAT was ruled and WHEN — rounds ~14 through 32. The *reasoning*
lives in the dated comment blocks in `src/engine/config.ts` (and, for a few
items, in the test files that pin the behavior); this file exists so an
agent can see a knob's history without reading that whole file first.
Append-only — when a rule is reversed or re-tuned, add a new line, don't
edit the old one. Round numbers and dates come from `git log --oneline`
(rounds 14–19 landed 2026-08-03, rounds 20–23 landed 2026-08-04).

## Reversals at a glance

Knobs that were set, reversed, and re-set — the ones worth double-checking
before assuming "current value = only value this ever was":

| Knob | Round 14–19 | Round 20–21 | Round 22 | Round 23 |
|---|---|---|---|---|
| `STAKER_FLOWS.FIGHT_RAKE` | 0 (round 16: "a PURE POT... win +entry, lose −entry") | 0 | **0.02** (2%, part of the LT-flywheel widening) | **0** again ("BACK TO ZERO" — 3,657 GP/mo to stakers was "enough... it seems") |
| `ECONOMY.GACHA_ROLL_PRICE` | 80 GP | 80 GP | **16 GP** (cut so bots would finally buy) | **80 GP** again (gacha out-supplied breeding 8:1 at 16 — "stables [should be] primarily breeding to create birds") |
| Gacha egg tiers (`GACHA_BIRDS`) | Blue + Purple + Gold all drop eggs (round 14 constrained their STATS, not which tiers drop) | same | same | **Blue removed** — Purple/Gold only; halves the egg rate (16/100 → 6/100) |
| Claimer sale on an unmatched (odd-bird-out) entry | sells anyway, no fight required | same | same | **reversed** — "no fight, no claim": an unmatched claimer refunds instead (fee + every claim) |
| `PINTAKASI.ENTRY_FEE` | 200 GP (round 18 launch) | 200 GP | **0 — FREE**, qualification-by-fighting replaces payment | 0 (held) |

## Round-by-round

**Round 14** (2026-08-03) — the naming law: no bird fights under an auto-name. Gacha egg-drop weights retuned (2 free pulls/day × a 23% egg rate was out-producing the breeding barn — "Mystery Egg (Blue) VIII" in the feed was the tell); ~16 eggs/100 rolls after. Gacha birds' stats CONSTRAINED so no tier's `statMax` beats the starter ceiling by more than a whisker (the old Gold tier at 350–700 strictly dominated gen-1 breeding) — the gacha's jackpot is STARS, never raw stats. Pixel-art coats added (base coat + element-tinted trim); a bred chick inherits a parent's base coat with a small mutation chance (placeholder genetics). **Paid gacha rolls start feeding the juice pool** — this closes the first of the two silent-burn incidents AGENTS.md's "GP is never printed or burned" rule calls out: gacha spend used to vanish rather than route anywhere (the second incident is `buyLand`, not fixed until round 22 — see below).

**Round 15** (2026-08-03) — cold start: every stable begins with 4 age-0 eggs, nobody fights week one. ONE lifetime record (round 9's practice/juvenile split collapses back into a single win/loss column that counts everywhere). Ages freeze at retirement. Colored letter grades (O/O+ tiers) in the Birds table. "Studding" status added.

**Round 16** (2026-08-03) — topline diffs (a snapshot per tick, day or week span) power the admin's before/after cards. THE FIGHT ECONOMY PRINCIPLE is stated here for the first time in these terms: standard fights print no GP — pure pot, win +entry / lose −entry, the subsidy is Land Tokens not GP. This is the "round 16 ruled" baseline that round 22's fight rake later breaks and round 23 restores (see Reversals table).

**Round 17** (2026-08-03) — The Card schedule view. Cancellation fixes: a per-farm lobby cap, and auto-play cards by style (shared logic with the bots, `formatScores`/`bestFormat` in `bots.ts`).

**Round 18** (2026-08-03, ruled 2026-08-03) — **THE PINTAKASI** launches: three weekly blade championships, one per "distance" (Long Knife + Short Gaff always run; the middle blade rotates Short Knife / Long Gaff by week parity). Hardcore throughout, age 3+, entry fee 200 GP (`LAND_BASIS` still carries this number today, held as "the stake the crowns represent" even though round 22 made entry free). Committee-seeded bracket (1v16, 8v9…) ranked by earnings → wins → avg figure; byes to the top seeds; the Selection Committee live-bumps a full field's weakest entrant. GP to the top (purse = the week's juice-pool share, first-round losers zeroed), land to the fallen (elimination grants pay the earliest-out the most). One bird per stable per week at launch (see round 19).

**Round 19** (2026-08-03) — demo polish. **One bird per CROWN, not one per stable** — the round-18 rule stopped after a single weekly entry, which starved a three-crown week down to one field of seven across ten farms; now a barn declares a specialist per blade. Class ladder reads STAKES wins (not lifetime wins) so maidens exist again — the discovery year doesn't graduate anybody. Player-side claiming. Starter TALENT SPIKE: each stat gets a small chance to roll above the starter band, so ~a third of birds show a green grade somewhere on day one (the six-stat average still sits in B/B+ — raising the whole bird stays breeding's job).

**Round 20** (2026-08-04) — **Pit Figure REBUILT, PFL-true.** The old figure scored each bird against its opponent, which let a loser out-figure the winner of the same fight; now the winner's figure is absolute (measured against a maxed-out ghost pace) and the loser is scored down from the winner by beaten lengths — a loser can never figure above the bird that beat it. Crown day **moves Wednesday → Thursday** (a bird gets one more ordinary card first). **MAX_PER_BARN: 1 → 3** — a deep barn may load one championship with three specialists, not one. Juveniles fight juveniles only (previously mixed into the open-age lobbies). Eggs hide their stats on the board and just read "Egg". 1 free gacha pull/day. World genesis seeds the juice pool with `SEED_JUICE` = 2,400 GP (3 days of drip) so the first championships are worth entering before breed fees fill the pool.

**Round 21** (2026-08-04) — the Staking tab: total LT staked and GP earned via staking, per barn and world-wide. This is a measurement round, not a rule change — but the numbers it exposed (ten barns, 10,627 LT staked, 55.96 GP earned across 35 days — breed fees were the pool's *only* inflow) are the direct cause of round 22's LT-flywheel ruling.

**Round 22** (2026-08-04) — **the LT flywheel**: every way GP changes hands now pays the land stakers a slice of the gross, in centi-GP. `STAKER_FLOWS`: fight pots 2% (reverses round 16 — see Reversals table), claim tags 2%, gacha spend 10%, breed fees doubled 2.5% → 5% (2.5% was round 10's original rate), land purchases 100% (previously silently burned — the second of the two silent-burn bugs AGENTS.md calls out). Gacha repriced 80 → 16 GP with bots/auto-play finally buying paid rolls. **The Pintakasi goes free-entry**: `ENTRY_FEE` 200 → 0, qualification earned by fighting instead (`POINTS_FOR`: juvenile 0 / real 1 / hardcore 2 per win; `QUALIFYING_POINTS` = 3). The Selection Committee's bump-line ranking switches to POINTS-first (previously earnings-first) — campaigning beats a fat wallet.

**Round 23** (2026-08-04) — the biggest round in the range, several reversals at once (see Reversals table for the full before/after):
- Fight pots **back to 0% rake** ("we are getting enough LT yield it seems" — round 22's 2% took monthly staking income from 56 GP to 3,657). The claim rake (2%) *survived* — a sale is a different thing from a fight.
- Gacha **back to 80 GP** and Blue no longer drops an egg (Purple/Gold only) — gacha was out-supplying the breeding barn 8:1 at 16 GP; the roll is a luxury again, not the cheap way to fill a barn.
- **100 LT to stand a stud** (`COVERS.STUD_LISTING_LT`) — the first LAND SINK. Round 22 gave land a strong yield with nowhere to spend it; this gives the yield a price to be measured against.
- Starter stars **nerfed**: `STARTER_MAX_HALF` caps day-one stock at 1.5★ (was uncapped enough that a 4★ bird could appear in week one). The gacha's Purple/Gold tiers and breeding are now the only way to 2★+.
- **CARRIAGE added** (`CARRIAGES`, `carriage`, `carriageHalfStars`) — the Ground/Air preference axis, PFL-style (dirt/turf, but here pang-baba/pang-itaas). Data layer only: seeded, inherited (`BREEDING.CARRIAGE_LEAN_STRONGER` = 0.75, higher than the element lean since carriage is meant to be selectable), rolled in the gacha — **not yet wired into the fight engine** (Zane's explicit scope call). See `src/engine/juvenile.test.ts`/`carriage.test.ts` for the round-23 coverage this file's job was to close.
- **The Juvenile Championship** (`JUVENILE_MAJOR`) — a full discovery-year ladder (maiden → stakes → claimer, gated on juvenile wins since a one-year-old has no stakes record) plus a non-hardcore weekly crown. Two blades (one knife, one gaff, lengths alternating by week), runs Wednesday, the day before the Majors. **NOT hardcore** — the one deliberate exception to "every championship force-retires its losers" in the whole game, because ending careers at age one would strangle the population the Majors are meant to inherit. Takes `JUICE_SHARE` = 20% of the juice pool, split across its two crowns; the Majors take whatever's left. A juvenile win does not bank a `stakesWins` (it's still the discovery year — round 19's "the discovery year doesn't graduate anybody" rule, held).
- **No fight, no claim** (reverses prior behavior — see Reversals table): an unmatched claimer used to sell anyway; now it refunds instead, fee and every claim.
- Cousin bot stables added with handler names (`bot-marco`, `bot-reno`, `bot-kevin`) alongside two new speculator styles — the whale (gacha bundles) and the landlord (maxes the daily land-buy cap) — giving both the gacha and the land market bots with an appetite for them (AGENTS.md's "bots and auto-play need teaching" rule: a door nobody has an appetite for measures zero in simulation, as happened twice before — claiming in round 19, paid gacha rolls in round 22).
- AGENTS.md gains the rule that pins the Handbook (`src/app/wiki/`) to move with the code in the same unit of work as any rule change.

**Round 24** (2026-08-04) — tooling round (the doctor, `bun run doctor`), plus the **daily Element weather** feature (`WEATHER.EDGE`, `weatherOfDay`): one element ascendant per game-day, boost-only. The edge shipped at a flat +1 and was cut to 0.25 the same round after review — the first appearance of the "read every flat modifier against ROLL_DIVISOR, never against the dice" lesson.

**Round 25** (2026-08-04) — **the fight balance lab** (`bun run balance`, `src/engine/balance/`): 14 measurement cases over the pure combat engine, the middle layer between unit tests and simulation. A measurement round in the round-21 sense — no knobs moved, but its gap report (the previous edition of BALANCE.md) is the direct cause of every round-26 ruling. Also `BATTLE.ELEMENT_EDGE` 1 → 0.5 (the flat era's last value — superseded by round 26's star scaling).

**Round 26** (2026-08-04) — **the big balance round.** Everything below was measured before and after by the lab; BALANCE.md carries the numbers.
- **Blades enumerated B1–B4** (keys and labels; the sabong weapons live on in `flavor`). Ordered sprint → marathon so a future fifth blade slots in and the MIDDLE blade becomes the balance anchor — Zane wants an odd blade count where the midpoint weighs every stat evenly and tuning works outward.
- **Station rebuilt: gate → slope** (`BATTLE.UNDERDOG_CLAWBACK` = 0.5 replaces `UNDERDOG_RATIO`/`STATION_DIVISOR`). The old binary gate INVERTED the grade ladder (a +100-per-stat bird lost 59–67%) and carved two one-point cliffs. Now the outmatched bird claws back station/2000 × 0.5 × the gap's per-roll value — capped below the gap (a superior same-shape bird is ALWAYS favored; Zane: "otherwise there's no point in breeding"), smooth from zero, station excluded from the totals it compares (heart, not class — counted in, the stat self-cancelled).
- **Stars reworked into the element's volume knob** (Zane's declared intent since round 25's report): both element edges scale by halfStars/10; the flat `BOOST_PER_FULL_STAR` stat boost is DELETED. 0★ = the element is a color on the card; every half-step is a real rung (the old floor threw half the ladder away). `ELEMENT_EDGE` 0.5 → **1.0** and `WEATHER.EDGE` 0.25 → **0.5**, re-read as CEILINGS — a 2.5★ bird gets exactly the old ruled values, only 5★ sees the full edge.
- **`BATTLE.ROLL_DIVISOR` 400 → 85**, tuned at B3 (the future midpoint) against Zane's grade targets: +100 on every stat ≈ 80% win, +200 ≈ 98%. B3 measures 82.9 / 97.4. The blade ends miss by ±10 — turn count amplifies stats — and that spread is the deferred phase-weight rework's job, not this knob's.
- **Condition re-ruled as intended** (the PFL-Temper theory): the boost is not a bug, condition is the wildcard stat — targets no distance, makes everything else arrive reliably, can offset a stat weakness. Comment and Handbook updated; no mechanic change.
- Figure fixes riding along: the loser is scored down from the winner's CLAMPED figure (a maiden flattened by a monster used to post 145), and floored at 0 (the round-25-recorded −5 bug).
- Doctor's weather-timing metric counts STARRED entries only (a 0★ bird has no going to play), and `weatherMatched` in bots requires stars for the same reason.
- Deferred, explicitly: the phase/blade-weight rework (waiting on the new blade lengths), Crowd Noise (station's parity role), logarithmic breeding speed, and any tuning of the 5★ stacked ceiling (95.8% on B4 with wheel + weather — watch it as the population's stars climb).

**Round 27** (2026-08-05, two commits) — **the five-blade dial and the distance rework.** Zane's PFL distance theory made mechanical; every number verified by the lab, before and after (BALANCE.md rewritten).
- **B5 added** (Needle Gaff, 45 turns, `damageMult` 2, `critMult` 1.05) — the fifth blade the round-26 enumeration was FOR. B3 (20 turns) becomes the dial's true middle.
- **The crown schedules go FIXED**: the Majors run B1/B3/B5 every week (Sprint / Middle / Classic, PFL-style; the round-18 middle-blade rotation dies), and the Juvenile Championship runs B2/B4 every week (the round-23 parity rotation dies). Between the two stages, all five blades crown somebody every single week.
- **The weight matrix replaces the absolute phase windows** (`FORMATS[].weights`). Every turn roll blends ALL FOUR distance stats, weighted per blade; one key stat per off-center blade (B1 agility=Start, B2 sight=Speed, B4 stamina, B5 gameness=Finish) and **B3 keys NOBODY** — measured even to 0.4 points, and no same-total specialist beats a flat bird there. The matrix is symmetric across the dial (agility's column mirrors gameness's, sight's mirrors stamina's) — PFL's distance symmetry as a config property, audited by the lab's `reach` case. `PHASES` survives as narration only (break/open/deep are chapters, not stat switches).
- **Wind goes UNIFORM at 100** (`BATTLE.WIND`; `BASE_WIND`/`WIND_PER_STAMINA` deleted) — Zane: "I'd make HP uniform across all birds (e.g. 100 HP), and then build the stats from there." No stat buys hit points anymore; every `damageMult` rescaled (13/7/4.5/3/2) and `GHOST_PACE` recalibrated (starters figure ~52 everywhere).
- **Stamina becomes the FUEL TANK** (`BATTLE.FUEL`: 8 base turns + 0.014/point, wall factor 0.5) — the hidden-gas theory. Past the tank the bird HITS THE WALL: agility/sight halve; stamina and gameness never fade (grit is mental). Old decay (`DECAY_PER_TURN`/`DECAY_FLOOR`) deleted — the lab had measured it decorative (≤0.3 points). Measured wall rates: 0% of B1/B2 fights, 37% B3, 85% B4, 99.5% B5. Honest split: stamina's direct weight carries most of its value; the wall adds 2–4 points on the middle blades and the "running on heart" story everywhere long.
- **`statScale` — the per-blade loudness dial** (1.5/1.15/1.0/0.9/0.8): scales everything the bird brings (blend, clawback, deep-water gameness bonus, BOTH element edges) so one grade of breeding buys the same win rate at every distance. Closes round 26's ±10-point blade-end spread: +100 now pays 76–85% on every blade, +200 pays 92–98. The one accepted miss: B1's +200 (91.8 vs 98) is a variance floor — five turns of dice cannot deliver 98%, and the sprint stays the upset blade by identity. Side effect, free: the 5★ wheel+weather stack eases 95.8% → 94.9%.
- Gameness KEEPS its morale job (the quit check and the low-wind bonus) — pure-distance-stat was considered and rejected; the run-or-die drama is sabong.
- `intent.ts` re-ruled to the round-27 map (B3 gets an `even` intent judged on spread, not order); all five blades now MATCH intent — round 26's "targets to measure against, not tune to" stance deliberately reversed, because the middle exists now.
- Bots' `formatScores` now reads the engine's own weight matrix (a weights retune re-teaches every bot for free). Figure guard: a winner never posts below one band.
- Deferred, still: Crowd Noise (station's parity role), logarithmic breeding speed, and the 5★ stacked ceiling watch.

**Round 28** (2026-08-05) — **THE FOG COMES DOWN: stats hidden until retirement.** Zane's ruling, closing the oldest contradiction in the game: the MCP prose had said "the skill is DISCOVERY" since round 13, while get_bird handed every owner the full sheet — and the bots proved the loop dead by carding perfectly from day one.
- **The six fighting stats are `null` on every live bird's view** (egg or active) and REVEAL at retirement, whatever ended the career — manual, hardcore loss, or the age cap. The reveal is announced in the retire event ("The sheet is public: <grade> overall"). Gacha pulls hidden too — round 14's "the jackpot is STARS, never raw stats" becomes literal. Stars, element, carriage, sex, age, record and figures stay visible: they are the card, not the sheet. Admin stays omniscient (it reads raw rows — the dev inspector, not a player surface).
- **The SCOUT REPORT** (`Lobbies.scoutReport`, `SCOUT` config block): per blade, the record + average/best figure + a score SHRUNK toward PRIOR_FIGURE (50, the even-starter figure) by PRIOR_WEIGHT (2) pseudo-fights — one lucky 80 must not out-rank three honest 60s, and an unraced blade reads "unknown, average", never "bad". Surfaced on get_bird; players, auto-play and bots all read THIS, identical evidence, no side channel.
- **The bots stop cheating**: `formatScores` (true stats × the weight matrix) is deleted. `bestFormat` now explores — while any blade has fewer than MIN_READS (2) figures, EXPLORE (35%) of cards go to the least-read blade, because without forced exploration a bird's first blade is self-fulfilling. Otherwise argmax scout score with JITTER (10) of judge error. Crown declarations sort on scout scores, deterministic (nobody experiments in the ring that retires losers), with round-robin spread on exact ties — a barn of unraced veterans would otherwise pile its whole roster into one crown.
- **The stud market gets the sheet**: StudView carries the revealed six stats + overall grade — every stud is retired by definition, and the revealed sheet is the sales pitch. Breeding is the INFORMED decision; fighting is the DISCOVERED one. (Grades now appear only on retired birds and studs.)
- **The doctor grows a discovery section** (omniscient by design): % of entries carded at the bird's TRUE best blade by age bucket — the answer key the players lost — plus juvenile blade coverage as exploration's adoption check.

**Round 28a** (2026-08-05) — **the overall grade is the fog's one exception, and the pairs question gets measured.** Two small rulings from Zane, one of which turned into the biggest open item in BALANCE.md.
- **`overallGrade` is ALWAYS public** — on eggs, on juveniles, on live fighters, not just on retirees and studs. Zane: "it's a small hint at the fighting strength of the bird, and it doesn't indicate how to place the bird into fights." That is exactly the split the fog is drawn along: the grade says HOW STRONG (six-stat average as a letter), never WHAT SHAPE — a B+ sprinter and a B+ stayer read identically, so it cannot answer the blade question discovery exists to ask. It also gives a fresh hatch something to be proud of on day one and the claiming ring an honest read on a bird with no figures yet. Computed in `Flock.view()` from the raw row, pinned by a test, taught in the Handbook and the MCP doctrine.
- **The scout report is confirmed as "fight history, shrunk"** — no new data, no engine internals: per blade the record and the figures the bird actually posted, plus a score pulled toward a neutral prior so a one-fight blade cannot out-rank a three-fight one. Bots read the same object a player does.
- **The `pairs` balance case** (`bun run balance pairs`, ruling PAIR_INTENT): three tables — the RANGE a pair covers, how much of the sum of its parts a pair keeps, and a shape comparison of a pair against a single spike carrying the same total surplus. **The control is always a FLAT bird.** The first cut of this case measured pair-vs-spike at equal totals (both birds carrying surplus) and warned on all six rows; Zane corrected it — that is the "which is the better way to spend 200 points" question, which he did not ask, and single-stat spikes are a fine line anyway because stars, station and condition are still on the table. Corrected, the hypothesis CONFIRMS: `[450, 450, 350, 350]` measures 70.8 / 69.2 / 68.2 / 59.0 / 56.2 across B1–B5 — two homes, a good middle, and no blade below 55% for any pair. Range costs about 5 points of peak and buys back about 5 at the second home and at B3 (spike spreads 16.7–31.8, pair spreads 9.7–25.3). **B3 turns out to be every pair's blade**: 67.9–69.4 for all six, because the even blade credits both halves of a two-stat bird while a spike collects on one — so the case exempts the even blade from its home check (read off `intent.ts`, never hardcoded). Verdicts are interval-honest: a naive top-2 check warned four times on gaps of ~1.5 points inside a ±2.1 interval.


**Round 29** (2026-08-06) — **the discovery loop measured, and the flock finally bred for shape.** Round 28 built the fog, the scout report and the doctor's discovery audit, then reported a scout accuracy near chance. Three separate things were wrong, and only one of them was the engine.
- **`SCOUT.PRIOR_FIGURE` 50 → 30, because 50 was never measured.** The comment claimed it was the figure an even fight between starters produces — round 27's uniform-100 wind rescaled every `damageMult` and the level calibration went with it. The `symmetry` control reads **26.9 / 27.1 / 29.3 / 30.2 / 31.5** and the live world's mean normalized figure is **32.1**. A prior 18 points above reality meant every blade a bird had actually fought got dragged toward 32 while every blade it hadn't stayed parked at 50: **evidence lost to ignorance, by construction.** Replaying the 5,505 entries of `sim-20260806-1318` with only this number changed moved scout accuracy **26.5% → 31.2% exact** and **52.2% → 56.0% on-or-adjacent**; `PRIOR_WEIGHT` at 0.5/1/2 all landed within 0.3, so this was the prior, not the Bayes. The proper fix — re-tuning `GHOST_PACE` so ~50 really is average, which would also stretch live figures off their current ~5–55 band and shrink `NOISE` relative to the signal — is **deferred to its own round**: it moves `GHOST_FIGURE`, `CLASS_BASE`, `MAX` and the Handbook's figure pages together, and an S+ specialist already reads 110 against a clamp at 150. The stale claim is now marked ⚠ MEASURED in `config.ts` rather than left standing.
- **Two things were tested and rejected, so nobody retries them.** *Normalizing the result out of the figure* (subtracting the win/loss means, 55.3 / 8.9) DROPPED accuracy 31.2% → 23.2%: winning at a blade IS blade-fit evidence, and stripping it strips the signal. *Weather normalization* is worth only **+0.2 to +0.8 points** — real, but not the bottleneck, because only ~25% of entries are timed and stars scale the effect. It needs no schema change whenever it is wanted (`weatherOfDay(dayIndex)` is a pure function of a column already stored).
- **The doctor's discovery audit was measuring the wrong thing, three ways** (found in review with Droid GPT-5.6 Terra).
  1. **Tournament bouts are not blade decisions.** Round two of a Major has its format fixed by the bracket; counting it graded the committee's schedule as the stable's judgement. Decisions now filter on `tournamentId === null` — bracket rows still feed the scout history, because a bracket fight is real evidence even when it wasn't a choice.
  2. **The verdict graded raw selected-format hits.** A card lands where the scout said AND where `EXPLORE` sent it AND where the lobby had room, so "hits climbed with age" could be true of a report that taught nobody anything. The verdict now grades the **scout's own top-ranked blade against its own random baseline**.
  3. **The denominator was full of coin flips.** The population's home-blade margins measured **p10 1.9 / p50 11.1 / p90 28.3** weighted stat points — *half the flock had no home blade worth finding*, and grading a scout on those birds reports noise as failure. New `DOCTOR.DISCOVERY_HOME_MARGIN` (10) prints a clear-home line beside the raw one: on the same logs, **31.2% → 35.6% (≥10) → 47.6% (≥25)** against an unmoved 20% baseline. The scout was never as blind as the number said. Both denominators print, because the gap between them is the population-shape story and it belongs in the report rather than hidden by it.
- **THE THREE BREEDING SHAPES (Zane's ruling).** `BREEDING_SHAPES` — the ADJACENT pairs on the distance dial: **agility & sight, sight & stamina, stamina & gameness** — derived from `DISTANCE_STATS` order, never listed. A line aims at a pair FIRST, then takes station, condition and element stars where it can. Why adjacent pairs: the round-28a `pairs` case measured it — a pair is strong at both blades its stats key, still ahead of a flat bird in the middle, and **never below ~62% at any blade where a single spike drops to ~55% at the blade it guessed wrong**. Under the fog, that floor is what a breeder actually owns. Single spikes remain a legitimate, narrower line.
- **The bots stop shuffling and start shopping** (`BREEDING_PLAN`, `foalScore`). `playFarm` took the first legal cover off a shuffled list — the same door-with-no-appetite failure as claiming in round 19 and paid gacha in round 22, and the direct cause of the flat flock above. Every barn now carries a `housePair`, prices every legal (hen, stud) pair on the expected foal (the parents' midpoint — public arithmetic a player can do, since both parents are retired) and buys the best, falling to its second choice when a cover is refused. Four terms: **shape 0.6 / level 0.5 / anchors 0.25 / stars 15 per half-star.** The shape weight deliberately *inverts* the measured win-rate ratio (a level point is worth ~0.30 win points against a separation point's ~0.18) by about 3×, because the two terms are not under equal pressure: level rises on its own as every barn stands its best retirees at stud, while **nothing pushes back on shape drifting to zero**. An unbiased plan leaves the flock exactly where round 29 found it. Star weight is *low* on purpose — the wheel is symmetric (one element in five is prey, one is predator), so a big star rating is near EV-neutral except that it can be TIMED. House shapes are spread UNEVENLY across the roster: an equal split would make every shape equally cheap forever, and scarcity is a price signal.
- `StudView` gains `halfStars` (the number beside the "2.5★ Fire" string) — stars are the wheel's volume knob, so a shopper comparing bloodlines wants arithmetic, not a parse. The Handbook gains a **"What to breed for"** section and the MCP instructions gain the shape doctrine, both computed from `BREEDING_SHAPES`.
- Still deferred: `GHOST_PACE` recalibration (above), Crowd Noise, logarithmic breeding speed, and the matched multi-seed current-vs-end-first discovery policy comparison — which was **not run this round on purpose**, because comparing two policies through a broken audit would have measured the audit.

**Round 30** (2026-08-06) — **the Pit Figure gets a UNIT, and the flock gets a pedigree.** Round 29 deferred "recalibrate `GHOST_PACE`" as its own round. This is that round, and the recalibration turned out to be the wrong fix: the scale did not need moving, it needed *anchoring*.
- **THE PIT FIGURE REBUILT — spine × night.** Zane's diagnosis (2026-08-06): "Going in the direction of DPS vs. a fixed target dummy was the right call, but somehow better defining what it's supposed to be should help us design and tune it." The missing piece was the UNIT. In PFL a figure is anchored to seconds at 8 furlongs, and that one physical fact means a grade step is a fixed number of points forever and the scale *cannot* drift. Rounds 20–29 only looked like that: `pace / GHOST_PACE × 100` reads as "percent of a maxed bird", but `pace` was wind DEALT, which depends on who you fought — so it was never measured against a fixed thing. Two proofs it wasn't: it **drifted** (round 27 rescaled the wind and every figure in the game moved ~20 points, silently), and **we had already patched around it** (`CLASS_BASE`/`CLASS_DIVISOR` existed only because pace lost the quality signal — turns roll on the DIFFERENCE between two books, so two maxed birds trade exactly as much wind as two starters). The ghost was never a bird; it was a divisor named after one. The rebuild: the **SPINE** is the bird's weighted stat blend at that blade on a fixed scale, dice-free and opponent-free; the **NIGHT** is the ratio of the roll bonus it actually rolled to what a `NOMINAL_CONDITION` bird would have rolled (condition, the element wheel, the day's weather, station's clawback and the fuel wall all land here); a loss is marked down by beaten lengths as a **SHARE** rather than a flat subtraction; one shared track variant fogs both sides so the fog never reorders the result. **Deleted:** `GHOST_PACE`, `GHOST_FIGURE`, `CLASS_BASE`, `CLASS_DIVISOR`, `BEATEN_SCALE`, `MIN_BEATEN` and `MAX` — the last on Zane's ruling, "let's forget about the 0–150 range, I don't think it's helpful to cap pit figures like this." A bred monster posting 140 should read 140.
- **Two anchors that turned out to be the same anchor.** Offered "a flat 1000 bird at its best blade posts 100" or "the grade step is the peg", Zane chose the first — and because the spine is LINEAR in the blend, both are true at once: `PEG_STAT` 1000 = `PEG_FIGURE` 100 means a letter grade (100 stat points, `grades.ts`) is worth exactly **10 figure points at every rung of the ladder**. The PFL analogy — a grade is a fixed 1.0 seconds — stopped being aspirational and became literal.
- **The happy accident: blade fit was already multiplicative and nobody had noticed.** Every format's weight matrix sums to exactly 1.00 (round 27's symmetry audit), so a FLAT bird blends identically at all five blades — cross-blade comparability *by construction*, with no hand-tuned per-blade table, which is precisely the job `GHOST_PACE` had been doing by hand. And a SHAPED bird's fit falls out proportionally with no fit term written anywhere. Zane asked for a ~25% multiplicative swing; the weights already delivered it. Measured on a true specialist (pair +200, off-pair −200), home−middle across five levels: **11.2 / 11.6 / 10.5 / 9.6 / 7.5** at base 320/500/800/1200/1600 — near-flat, where round 29 measured the old additive fit *collapsing* 11.9 → 3.5 as birds improved. Home−worst GROWS with the bird: 23.8 → 32.0. The better the bird, the louder its shape. Full table in the `FIGURE` block.
- **The same lesson, learnt twice, now fixed structurally.** Rebuilding the figure moved the scale under the scout — exactly what round 27 did and round 29 caught. Both scout constants were fitted to the figure's OUTPUT, so both went stale:
  - **`SCOUT.OWN_GRADE_STEP` 15 → 10**, and it is no longer fitted at all — it is **derived** from the figure's own construction (100 stat points / `PEG_STAT` × `PEG_FIGURE`). Measurement now confirms rather than sets it: B 21.4 → B+ 27.1 → A 37.1 over 5,588 fights. `src/engine/scout.test.ts` **pins the derivation**, so a future re-peg of `PEG_STAT`/`PEG_FIGURE` fails loudly instead of quietly leaving the scout 18 points adrift.
  - **`SCOUT.OPPONENT_GRADE_STEP` 5 → 0.** The opponent has left the figure entirely — the spine reads the bird's own blend, so there is nothing of the opponent to remove. Measured, holding own grade at B+: 27.0 against B+ company, 27.8 against B, 25.8 against A — flat inside the noise. Kept as a knob at zero rather than deleted, because beaten lengths still carry the opponent faintly and a future rule that pays for company would want the dial back.
  - **`SCOUT.PRIOR_FIGURE` 30 → 27** (the measured B+ mean). This one genuinely **cannot** be derived, and the reason is worth recording: the spine says a B+ bird should figure ~35, but the live flock averages 27 because its CONDITION is starter-grade (~320 against a nominal 1000), so nearly every bird fights below nominal form and every loser is marked down on top. Those are properties of the POPULATION, not the formula — so this number will drift UP on its own as the flock breeds up. Re-measure it when the BLOODLINES ladder shows the mean grade has moved a band; do not fit it to a formula it does not obey.
  - `src/engine/balance/cases.ts`: `FIGURE_GRADE_STEP` 5 → derived (10), with a 10% tolerance for measurement quantization, and `GRADE_BAND` is now exported from `src/engine/grades.ts` so nothing re-types the literal 100.
- **The payoff, measured.** Fixing the scout's calibration moved discovery hard. On the "clear home" verdict (birds whose home blade actually beats its runner-up — the scout is not graded on coin flips), **age 4+ went 30.5% → 57.5%** against a 20% random baseline, 83.5% on-or-adjacent; the fresh round-30 world reads clear-home **57.3% (age 1) / 50.9% (age 2–3) / 57.5% (age 4+)** and raw accuracy 50.4 / 41.8 / 44.0. The balance suite's grade-calibration table now reads +10.1 / +9.9 / +9.7 / +11.5 / +11.6 / +11.7 / +11.5 / +11.6 / +12.2 / +10.9 / +10.8 / +10.8 / +10.7 / +10.7 / +10.4 against the **derived** +10 target — the engine keeping a promise its config makes, rather than hitting a number somebody chose.
- **GENERATION MARKER (Zane's ruling).** Starters and gacha pulls are generation 0; a chick takes its **dam's** generation + 1. New `birds.generation` column (`schema.ts` + `ddl.ts` hand-synced; worlds are remade, not migrated), **un-fogged** on `BirdView` — a generation is pedigree, not shape, so it says nothing round 28's fog protects — surfaced on MCP `get_bird`/`list_flock`, and a new **BLOODLINES** doctor section reporting per generation: count, mean grade, mean stars and median home-blade margin. The margin uses the IDENTICAL `homeBlade` arithmetic the scout audit is graded on, so "better tuned" means the same thing in both places. Latest 91-day sim, gen 0 → 1 → 2: **328.2 → 335.7 → 348.1** mean stat (+20.0), 1.75★ → 1.74★ → 2.06★, home margin 8.1 → 11.2 → 10.3. The loop compounds — slowly. Until this section existed there was no way to see whether it compounded at all.
- **HENS ARE BRED TO THEIR OWN SHAPE.** Zane: "Each hen is different, and ought to be bred strategically. If a hen has b1/b2 oriented stats, it should breed with a b1/b2 oriented rooster." Round 29 aimed every cover in a barn at ONE house axis, so roughly two hens in three were bred *across* their own grain and the foal's midpoint undid the dam instead of compounding her. The shape is now picked PER HEN off her own sheet (new `bestShape` helper), with **`BREEDING_PLAN.OWN_SHAPE_MIN` = 40** as the bar below which she falls back to the barn's axis. Measured, not felt: over all 149 breeding hens in the round-29 sim, best-pair separation ran p25 = 32, median = 55.5, p75 = 91 — so the genuinely shapeless third falls back and the rest is bred to itself. `housePair` was **demoted to a fallback rather than deleted**: a near-flat hen following her own noise is following nothing, and scattering a barn's covers across all three axes destroys the concentration that made round 29's uneven house spread a price signal.
- **The doctor's breeding ruler had to follow the ruling.** Round 29's adoption line measured every cover against the BARN's axis. Once hens were bred to their own grain that ruler was measuring a policy the bots had stopped following — it read dams at −9.0 and made a working plan look broken. Rebuilt to measure the **choice**: for each cover, how far the chosen SIRE sits along the DAM's own shape, against the floor of what an *unchosen* sire would score on those same axes. Latest sim: **sires chosen +67.0 against an unchosen sire +1.0**, foals landing at +59.5. Nothing but selection produces that gap. An honesty note worth keeping: the first version of the accompanying test asserted that covered hens carry MORE shape than the average bird. It passed on a 20-day world and is FALSE in a mature one (hens +59.0 vs any bird +64.5) — because the flock number is lifted by the plan's own foals while the breeding hens are mostly unselected founders. The assertion was **removed rather than tuned**.
- **Two footguns fixed, both found by being bitten.** (a) `latestSimDb()` picked the newest sim by NAME, so a hand-named keeper (`sim-20260806-season-c.db`) sorted after `sim-20260806-1636.db` and the doctor reported on a world three schema versions old; it now orders by **mtime**. (b) Worse, it did so SILENTLY — SQLite renders a double-quoted unknown identifier as a string literal, so the newly added `generation` column printed as the literal string `"generation"` for all 530 birds without a single error. `scripts/doctor.ts` now **refuses** a world whose schema predates the columns it needs, and says what to do about it.
- Still deferred, unchanged from round 29: Crowd Noise (station's per-fight stage level), logarithmic breeding speed + the years-to-max analysis, the B1 +200 spec retarget, and the matched multi-seed current-vs-end-first discovery policy comparison. **Newly opened:** Element in the breeding score — a cross-element cover should cost star potential so bots keep lines pure. Zane deferred it, suspecting the population is too thin and the star incentive too weak for it to measure yet.

**Round 31** (2026-08-06, two commits) — **THE CARD: fights are POSTED now, not conjured.** Round 30 left the rising unmatched rate as an open hypothesis blaming the scout. It was not the scout. `Lobbies.enter` created the lobby when the key didn't exist, so every fight type was available every day and the perfect fight always existed *because you invented it by asking*. Zane: "The on-demand lobbies of perfect fights is crazy lol. Never intended that." Measured over 91 days: **74 live keys taking ~70 entries a day, 2.9 birds per lobby against a capacity of 8, and 16.3% of entries never drawing an opponent** — decomposing as 35% sole entrant, 31% two barn-mates alone (matchmaking never pairs same-barn birds), 34% odd bird out. The first two are pure key-space damage; no matchmaker can fix them, only collision can.
- **THE KEY SPACE CUT 75 → 50, on three rulings.** (a) **Hardcore leaves the daily card entirely** — Zane: "There should be 0 hardcore fights outside the Finals. This is a regression in the fight schedule." It had already measured its own case: 201 entries producing 55 fights at a **45.3% unmatched rate**, the worst of any mode, for under one fight a day. `FIGHT_MODES` is now `juvenile | real` and lives in config (it had been a bare union in `lobbies.ts`, re-typed literally in four other places); hardcore survives only in the Pintakasi Majors, which are tournaments and never open a lobby, so `battleLog.mode` still carries "hardcore" for those. (b) **`nw2` merged into `nw3`** — of 181 active birds, nw2's exclusive constituency was **10 birds** and nw3's **18**: two classes and ten keys separating 28 birds. One conditioned rung now serves all 148 birds under three wins (`NW_CAP`). (c) **Claimer tags thinned**, `PRICES` 5 rungs → `[50, 200, 600]` and `JUVENILE_PRICES` 3 → `[25, 100]`. This was the single biggest fragmentation fix available anywhere: the tag is part of the key, so claimers alone were **40 of the 75 keys — 53% of the whole space off one axis** — taking 13.1 entries a day divided into **0.33 entries per key**. The dear rungs were not thin, they were dead: b3@400 drew TWO entries in 84 days.
- **A PUBLISHED DAILY CARD** (`CARD`, `cardOfDay`, `isOnCard`). ~11 keys a day, 10 on the Majors' crown day. Pure function of the day index, exactly like `weatherOfDay` — no schema, no storage, reproducible for any past day, and `cardOfDay(day + 1)` is free, so **tomorrow is public and plannable**. The PFL rhythm Zane asked for: "if you just wanted to wait for a perfect race, you might have to wait 3-4 days, this causes players to settle for close-to-ideal races." **Where the scarcity goes is the whole design**: every CLASS runs every day in both divisions and what rotates is which BLADES each class runs. Two reasons — the classes NEST (maiden ⊂ nw3 ⊂ open) and 33 of 181 active birds are open-only, so adult open must be posted daily or veterans strand (nothing may ever be stranded); and the blade is the DISCOVERY axis, so the shortage lands on the most interesting question a player has.
- **The rotating deck, and the two bugs it had.** Blades come from a deterministically shuffled deck walked with a per-class stride, so every blade is *guaranteed* once per deck rather than left to independent draws. (a) A slot group straddling a deck boundary drew from two independently shuffled decks and could pick the same blade twice, silently costing that day a lobby — fixed by skipping a blade already posted. (b) Thinning adult open on crown day by asking the deck for 2 blades instead of 3 changed the cursor stride and **desynced the rotation**, measured as the worst blade gap jumping to 5 — fixed by always walking the full count and slicing after (`take`). Measured gaps after: k=3 and k=2 classes reach every blade within **4 days**, k=1 classes within **8**, and the whole card within **3 days** in both divisions.
- **THE CHOOSER INVERTED — blade first, class as the slack.** The load-bearing design change, and it came out of that k=1 gap. The old `pickSpec` picked a class off the bird's record and then a blade; ported naively that starves a class running one blade a day, and **a juvenile's whole discovery year is 7 days** against an 8-day worst gap — a winless chick could have finished its career without ever seeing a blade in a maiden. Inverting uses the nesting: find the blade among everything the bird is *legally allowed to enter today*, then take the most PROTECTIVE class posted at that blade. A winless juvenile whose blade isn't in today's maiden simply runs juvenile open at that blade. **Maiden becomes protection you get when the card offers it**, and the whole-card gap (3 days) governs coverage instead of the per-class one.
- **ONE PREDICATE, TWO CALLERS** — `entryRefusal(bird, spec)` extracted, returning the reason or null. `Lobbies.enter` throws it; the chooser filters on it. The ladder had already been encoded twice (there and in `ladderClass`), and a third copy inside the chooser would have been the drift that kills: **both entry paths wrap `enter` in `quietly()`**, so a chooser proposing specs the door rejects would surface as nothing at all except a fill rate that silently collapsed. The card door is a BACKSTOP; the choosers are the mechanism.
- **UNBOUNDED LOBBIES** (Zane: "no more 8-max and then duplicating the lobby. Just let it grow infinitely."). `LOBBY.CAPACITY` and the `capacity` column deleted; exactly ONE lobby per posted key per day. Capacity-8 duplication was splitting a hot key straight back into two half-empty rooms — undoing what the card exists to do, in the same breath. It also removes any reason to CAMP: under a cap a late entrant could find the room full and be shunted into a duplicate with a different field, so watching and waiting paid; unbounded, you can always get in and the fill count you saw only grows. Two consequences recorded honestly: the round-17 per-farm seating cap **died with its denominator** (it was `capacity/2`) and was dropped rather than replaced — a barn that pours its roster into one key strands its own surplus, refunded in full, and the doctor watches same-barn stranding so we find out rather than assume; and **8 was EVEN on purpose**, so unbounded means parity is a coin flip and a lobby that closes odd still strands one bird.
- **`findOrOpenLobby` now MATCHES on `dayOpened`**, which it had always written and never read. Harmless only because `resolve` closes every open lobby each tick — the moment anything closes selectively (the real-time clock closes claimers early) a stale lobby could have seated tomorrow's entrants on a key that is no longer posted. The card has to be authoritative, so the day is part of the lookup.
- **`POINTS_FOR.hardcore` is now UNREACHABLE**, and every sentence teaching "or two hardcore wins" was already false. Crown points are written only from a lobby fight (`POINTS_FOR[lobby.mode]`) and tournament wins bank none, so the only route to `QUALIFYING_POINTS` is three real wins. Fixed across the wiki and the MCP prose. The fields **survived** losing the route: majors 29 run / 1 cancelled at a field of **11.0** (round 30: 9.6).
- **A PRE-EXISTING BUG FOUND AND FIXED FIRST, in its own commit (`d662d72`).** The crown-day door gated on `isCrownDay` (Thursday) but queried pending tournament entries **without filtering by division** — the division lives on `tournaments`, not the entry. So on WEDNESDAY a Juvenile Championship registrant was never blocked: it entered a normal lobby AND fought its crown the same day. Nothing caught it — the tournament stamps its `battleLog` rows with Thursday's day index so `checkFightCap` never saw both, and the doctor's one-card-per-bird-per-day invariant buckets on `lobbyEntries` only. Landed separately on purpose, so an inflated juvenile fight count wasn't read as the schedule change moving numbers.
- **THE RESULT, on a fresh 91-day world.** Mean birds per lobby **2.9 → 7.36**; entries never drawing an opponent **16.3% → 4.5%**; lobbies holding a single bird **334 (16.6%) → 26 (3.9%)**; same-barn-only stranding **~302 birds → 25**; lobbies 2,012 → 673. All five invariants pass and **0 health warnings** (round 30 ended on 2). Claimer keys went from 0.33 entries per key to roughly **4.4** — the marketplace got healthier, not smaller — with `claims placed` adoption holding at 14/15 farms.
- New doctor section **LOBBY FILL** (mean, a bucketed histogram, singletons, and same-barn stranding) with `DOCTOR.FILL_WARN` = 5, and `BotDayReport.noCard` counting birds the card had nothing for — the only error surface the bot layer has, since `quietly` swallows everything else. New `src/engine/card.test.ts`: 16 tests, ~11,700 assertions, including an exhaustive proof that **no shape of bird is ever stranded** on any day (enumerated over age × stakes record × juvenile record) and that eggs and retired birds are refused everywhere.
- **Next round, already scoped: MULTI-FIGHT LOBBIES.** The ~4.5% residue is the odd bird out and it is structural — but unbounded lobbies change its shape for the better. Round-robin across a whole room is impossible (30 birds = 435 fights), so the matchmaker should partition into GROUPS (Zane's FIFA group-stage analogy): a room of 30 becomes seven groups of four plus one of two, and everybody fights. Only a room holding a single entry would strand anyone. It needs `CADENCE.FIGHTS_PER_BIRD_PER_DAY`, the `potCents = ea.fee * 200` assumption, per-fight `landForFight` minting and the single-`battleLogId` shape of `lobbyEntries` changed together. **Zane's ruling: entry fees become divisible by 3** (40 → 42, 8 → 9) and the stake splits across the fights actually taken, refunding the remainder.
- Newly opened alongside it: the **element rework** — the wheel is additive so it fades as birds breed up; per-fight random weather; and showing a lobby's element composition, because the counter-meta is currently unplayable (the field is fogged). Also **PFL-style aging curves**, and **carriage** (Ground/Air) is still data-only and is the natural second star axis. Unchanged from before: Crowd Noise, logarithmic breeding speed, the B1 +200 spec retarget.

**Round 32** (2026-08-06) — **THE BREEDING BARN OPENS: `breedDrive` becomes DEPTH, not frequency.** Zane read the round-31 doctor and found three things wrong that the round-31 report could not see: half the broodmare band was idle, the two juvenile crowns were wildly lopsided, and juvenile open had headroom nobody was spending. All three are bot-and-schedule problems, not engine ones, and none of them would have surfaced without a number pointed at them.
- **THE ONE-COVER-A-DAY CAP WAS NEVER A BUDGET RULE.** `Bots.playFarm` ended its breeding block with `break; // one cover a day is plenty`, behind a `rng() < bot.breedDrive` coin flip. Do the arithmetic it was standing in for: a cover is `ECONOMY.BREED_FEE` (160) and a hen holds ONE pregnancy until her egg lays the following Friday, so ~10 retired hens want ~10 covers **a week** — 229 GP a day against an 800 GP `DAILY_DRIP`, under 30% of income. The cap was throwing away two thirds of the world's breeding capacity to protect a wallet that was never in danger. The round-31 sim measured the cost exactly: **73 of 154 retired hens had never bred once, while eight hens carried nine foals each, and 81 dams produced all 280 chicks.** Zane's ruling (2026-08-06): "All barns should basically want to breed, except for the ones with specific non breeding strategies like the one that aims to amass land tokens via purchase… I'd expect barns to either breed all of their hens, or their X best hens each season. They all have the same budget of 800/day which is five breeds." So `breedDrive` stops being a dice roll and becomes a SHARE: new `breedTarget(bot, hens)` = `min(hens, MAX_COVERS_PER_DAY, max(ceil(hens × breedDrive), floor))`. 0.9 works nearly the whole band, 0.3 works the best third — and since the pairs are ranked by expected foal, "the best third" means the best third. The stop is money and biology now, not a counter: GP is checked **per cover inside the loop**, so a barn that can afford three buys three and halts rather than committing to a target and overdrawing toward `RESERVE`.
- **THE EXEMPTION IS KEYED OFF THE APPETITE, NOT THE BOT.** The floor (`BREEDING_PLAN.MIN_HENS_COVERED`) is **0 for any bot with a truthy `landAppetite`** — the landlord pours its whole wallet into land every day and genuinely cannot afford a floor, and that is the "specific non-breeding strategy" the ruling carves out. Keyed off the appetite rather than a magic bot id so a future landlord inherits the exemption for the right reason.
- **TWO NON-OBVIOUS THINGS NEARLY MADE THE WHOLE CHANGE INERT. Both are the lesson of the round.**
  1. **The real cap was the PRICING budget, not the `break`.** The only limit in the pricing loop was `MAX_PAIRS_PRICED` (150) against `MAX_STUDS_PER_HEN` (40) — so barely **FOUR hens** were ever priced in a day. Lifting the one-cover rule on its own would have changed nothing past the fourth hen, because there was nothing priced left to buy. The loop now counts **HENS** (`coverTarget + HENS_PRICED_SLACK`, slack 3 for the covers that bounce on kinship or a stud already covered out for the week — `quietly` eats those failures, so without slack a barn just silently buys fewer foals than it meant to), and the pair cap goes back to being a pure runaway guard.
  2. **The first cut set `MIN_HENS_COVERED = MAX_COVERS_PER_DAY = 5`, which is arithmetically the same as deleting `breedDrive`.** With floor === cap, `min(hens, cap, max(share, floor))` collapses to `min(hens, cap)` for every barn that isn't the landlord: thirteen stables breeding identically while the config comment cheerfully describes three different styles. A subagent writing the tests **caught it, refused to write a vacuous assertion, and pinned the config identity instead** — which is exactly the right failure mode for a test to have. Now `MIN_HENS_COVERED = 3` (a floor on EFFORT, not a target: a barn with three free hens works all three whatever its style, which is enough that no good hen rots, while leaving room above for a broodfarm to be visibly a broodfarm) and `MAX_COVERS_PER_DAY = 8` (sized above one day's drip, so a barn that ever hits it is spending down its stake and something upstream is wrong — a brake against a bug, not a policy). A test pins the strict inequality.
- **CARRYING HENS COME OUT OF THE COUNT BEFORE `breedTarget` SEES IT**, and that is what makes depth mean anything. On any given day most of a worked band is already pregnant; counting the whole band pins every barn's target to the cap and then spends the pricing budget on hens whose cover can only throw — swallowed by `quietly`, invisible. Read off the db rather than `flock.all()`, because `BirdView` exposes `eggStage` but not `motherId` and the rule is specifically GESTATING: an egg already LAID leaves its dam free, which is round 14's rule letting a hen carry again while last week's egg waits to hatch. `BotDayReport.bred` becomes `string[]`.
- **THE JUVENILE CROWN CHASE DECLARES BEFORE IT SENDS — a real bug, and iteration order was the whole of it.** `chaseJuvenileCrowns` looped `for (const blade of blades)` on the OUTSIDE, sending up to `JUVENILE_MAJOR.MAX_PER_BARN` birds to each in turn. But `Tournaments.enter` allows a bird **one championship entry per week**, so b2 — merely by being checked first — took every barn's two best juveniles and b4 received only the overflow from barns holding three or more qualified chicks. Measured across the round-31 sim: **b2 fields of 16–27 against b4 fields of 1–8.** Nothing about the birds caused it. The adult `chaseCrowns` has always declared first for exactly this reason; the same structure is used here now — every bird picks its crown off its own scout read, **ties spread ROUND-ROBIN** (falling to the first blade on a tie would rebuild the imbalance the fix removes), then a second pass seats anyone whose declared crown was already full for their barn, because the juvenile stage costs nothing and is not hardcore, so a second-choice blade beats no blade. Zane's framing: "The whole point of juvi season would be to at least determine which extreme they are good at (B1/B2 vs. B4/B5), if it's a true B3 bird, that's a wont-solve scenario and they'll simply opt into B2 or B4 based on the data available." **After: b2 22–24 against b4 16–22.**
- **A THIRD JUVENILE OPEN BLADE**, `CARD.juvenile.open` 2 → 3, so the card is 12 keys a day (7 adult, 5 juvenile). At two blades the worst gap between two appearances of a blade is **four days** and a juvenile career is exactly **seven** (`canJuvenile` is age 1 exactly — a closed division, no second chances), so a chick could finish the only year it is allowed to experiment in having never been offered two of the five blades. Three closes the gap to two days. Affordable because juvenile open was the **fullest key on the board** — 8.41 birds a lobby in the round-31 sim against a 7.36 world mean. Zane approved it over a second MAIDEN blade, on the grounds that the starter population does not support the maiden split. The wiki's card page now **measures** the gap off four weeks of real cards rather than asserting it, so moving the slot count moves the sentence.
- **NEW DOCTOR SECTION — `broodmare band`**: what share of SETTLED retired hens have ever carried, plus the busiest hen's foal count, with `DOCTOR.BRED_BAND_WARN = 0.7`. It exists because the selection numbers beside it **cannot see this** — they grade the covers that HAPPENED, which were healthy all along, while half the band sat idle and nothing reported it. The threshold is deliberately well under 100%: the plan is SUPPOSED to reject hens, and a band where every mare carries would mean nobody is selecting. **An honesty note worth keeping:** the first cut counted every retired hen and read a real 76% as **64%**, because the sim retired 32 hens in its final week who had not been passed over — they had not had a turn. `DOCTOR.BRED_BAND_GRACE_WEEKS = 2` excludes them. One week would be the honest floor since covers are bought daily; two leaves room for a deep band to work through a backlog after a heavy Friday.
- **TWO PROSE REPAIRS, both found by subagents, both player-visible.** (a) `tournaments.ts` told a failing registrant to bank points via "a hardcore win" — **unreachable since round 31 took hardcore off the daily card**, so the game was sending players to look for a lobby that does not exist. The `POINTS_FOR.hardcore` knob survives in config against a future per-entry risk choice; the ADVICE did not. (b) The `CARD` config's own WHY comment still read "juvenile ~24 over 4 keys" — stale the moment the third blade landed, directly above the constant that changed. (c) `wiki/pintakasi/page.tsx` still said the juvenile juice slice is split across whichever blade "runs that week", a leftover from before round 27 killed the rotation; both crowns run every week. These are exactly the sentences CLAUDE.md's checklist step 3 warns about — a computed number fixes itself, a sentence does not.
- **AN ADOPTION GAP OF THE KIND THE HOUSE RULES WARN ABOUT, in the MCP server.** `enter_pintakasi` and `pintakasi_board` hard-coded `division: "major"`. `Tournaments.enter` has always taken a division — the tool simply never exposed it — so an agent-player could read the whole Juvenile Championship section of the instructions and **have no door to it**. Only bots could enter one. Both tools now take an optional `division`, defaulted to `"major"` so every existing call keeps its meaning, and the instructions gained the declaration advice (one crown a week, so a chick must declare; send it to the blade its scout report reads better at). Third instance of "a door nobody could walk through" after claiming (round 19) and paid gacha (round 22), and the first where the door was missing for the HUMAN-facing client rather than for the bots.
- **THE RESULT, on a fresh 91-day world: 0 warnings, 0 invariant failures.** Broodmare band **47% of 154 hens → 75.9% of 166 settled hens**, busiest hen unchanged at 9 foals. Bot covers **238 → 533**; hatches **501 → 761**; the supply line's covers **280 → 598**. Population: active **172 → 341**, retired 329 → 420, eggs 35 → 90. BLOODLINES gen 2 vs gen 0: **+23.4 mean stat / +0.4★ / +3.1 home margin → +30.0 / +0.4★ / +6.9**, gen-2 mean grade 351.4 → **357.9** and stars 2.15 → 2.19. Mean birds per lobby **7.36 → 9.94**, singletons 3.9% → 3.8%. Majors 29 run / 1 cancelled at a field of 11.0 → **30 run / 0 cancelled at 13.7**; Juvenile Championship field 10.3 → **14.0**. Flock shape: **53.0% → 57.0%** of birds clear the 10-point home bar, median home margin 10.8 → 12.2. Discovery clear-home **53.4% → 57.9%** at age 2–3 and **36.3% → 52.7%** at age 4+ — on a bigger denominator, which is what round 31 asked for. Staker-pool breed fees 2,240 → 4,784 GP. Hardcore losses 457 → 642 (a bigger population fights more). `bun test src` **343 → 357 pass / 0 fail**, 17,468 assertions across 25 files; `bunx tsc --noEmit` and `bun run build` clean; `bun run balance` unchanged at its standing 1 warning / 154 rows / 46 findings.
- **THE ONE NUMBER THAT WENT THE WRONG WAY: unmatched 4.5% → 5.7%**, even as fill went 7.36 → 9.94. Not spun: that is the odd-bird-out residue riding a much larger population — more entries means more rooms closing odd — and it is precisely the residue the deferred group stage removes, since a room partitioned into groups of four strands nobody but a lone entrant. It is the cost of the population the breeding change bought, and it gets paid back next round or not at all.
- **AN OPEN QUESTION, recorded because it was only half explored: SELECTION PRESSURE versus BAND UTILIZATION.** The mid-round sim — taken before the floor and the cap were separated, when every barn was covering to the same target — showed gen-2 mean stat gain **FALLING to +11.5** while home margin **ROSE to +7.8**. Covering more hens means covering less selectively: the marginal hen is by construction the worst one on the barn's list, and a plan that must cover her is a plan that has stopped choosing. Separating the knobs recovered it to +30.0, so the current setting is on the right side of the trade — but nothing here establishes where the peak is, and `MIN_HENS_COVERED` is now the dial that trades one against the other. Watch both columns of the BLOODLINES ladder when either knob moves.
- **Newly scoped, after the group stage: THURSDAY OPENS UP.** The Majors field gets seated automatically by the committee on **LIFETIME EARNINGS** — a RANK, not a fixed threshold, so it self-scales as the world gets richer and never needs re-tuning. That removes registration, and removing registration removes the reason to bar registrants from the Thursday daily card, which is the only reason `CARD.CROWN_DAY_OPEN_BLADES` exists (it becomes dead when this lands). **Zane's ruling on agency: auto-QUALIFY, opt-in to STAND** — a hardcore bracket force-retires its losers, so a bird is never entered into one without its owner accepting. The plumbing already supports it: `Lobbies.resolve` runs BEFORE the tournaments in the same tick, so a bird going 3/3 on the final Thursday card genuinely banks those earnings before the crowns seat; and `committeeCards` already computes lifetime earnings off the battle log, so no schema change is needed. Also raised by Zane and queued separately: **+5 bot farms (15 → 20)** — one line of config, but it moves population, so it takes its own commit and its own baseline rather than contaminating somebody else's measurement.
- Unchanged and still deferred: the element rework (additive wheel, per-fight weather, fogged fields), Crowd Noise, logarithmic breeding speed, the B1 +200 spec retarget, element in the breeding score, PFL-style aging curves, and carriage as the second star axis.

**Round 33** (2026-08-07) — no rules changed. The Stewards' Office round: compact records, graded birds, token-aware money columns, richer card and bracket views. Recorded here only so the round numbering doesn't look like it skipped.

**Round 34** (2026-08-07) — **THE GROUP STAGE: one entry stops being one fight.** The last structural cause of unmatched entries, and it could not be matchmade away. Round 31's card made entries collide and took unmatched 16.3% → 4.5%; round 32's breeding change nearly doubled the population and pushed it back to **5.7%**, which was recorded at the time as the price of the birds and the debt this round would pay. The residue was pair arithmetic: an unbounded lobby drawn into PAIRS strands the odd bird whenever the field is odd, and strands everyone when a room holds one entry. No matchmaker fixes that, because odd is odd.
- **A CLOSING LOBBY NOW DEALS GROUPS, NOT PAIRS.** `Lobbies.matchmake` is replaced by `dealGroups`: the field is cut into groups of at most `GROUP.SIZE` (4) and everybody fights everybody inside their group — Zane's FIFA group-stage framing, ruled after round 31. Barn-mates are still NEVER matched, so a group holding two birds of one barn simply skips that pairing and both fight fewer times.
- **SIZES ARE LEVELLED, NOT PACKED, and that is the whole design.** As few groups as will hold the field, then sizes evened across them: nine birds become **3+3+3, never 4+4+1**. Packing yields more fights (4+4+1 = 12 fights against 3+3+3's 9) and more full cards, but it re-creates the stranded bird this round exists to delete. Levelling guarantees no group of one while the lobby holds two or more. The cost is paid in SHORT cards, not in stranded birds — see the result below.
- **THE BIGGEST BARN IS DEALT FIRST.** Placing easy singletons first fills the groups and forces the collisions; the barn with the most birds in the room is the hardest to spread, so it goes while there is still room. Ties break on the lobby's seeded rng, so a replayed close replays.
- **THE STAKE SPLITS ACROSS THE FIGHTS (Zane's ruling).** "Just divide the total entry among them… If the bird is the odd bird out and only gets two fights, then I'd expect them to get refunded 20." The fee escrows whole at the door, each fight risks `stakePerFight(fee) = fee / (GROUP.SIZE - 1)`, and the unfought share comes home at settle-up. **`ECONOMY.REAL_ENTRY_FEE` 40 → 42 and `JUVENILE_ENTRY_FEE` 8 → 9** so they divide by three exactly — a 5% and 12.5% rise in the price of a NIGHT that buys three times the fights, so the price per fight fell hard (14 GP and 3 GP a side). ⚠ **The pot is two STAKES, not two fees.** `potCents = ea.fee * 200` became `stake * 200`; leaving it would have paid winners three times over out of escrow nobody put up.
- **LAND MOVED OFF THE FIGHT AND ONTO THE NIGHT.** It used to pay per fight on the entry fee; it now pays ONCE per entry at settle-up on `landForFight(stake × fights taken)`. Paying per fight would have tripled LT supply, and feeding the curve the per-fight stake instead would have flattened it — 14 GP and 3 GP both sit near the `max(1, …)` FLOOR, where the exponent does nothing. A juvenile shows it plainly: three 3 GP fights would each floor to 1 LT and mint 3 for a night worth exactly 1. Per-fight payment does not reward fighting, it rewards being cheap.
- **⚠ THE FEE CHANGE SILENTLY INVERTED A STANDING RULING, AND THE HANDBOOK CAUGHT IT.** Land rounds to whole tokens with a floor of 1, and moving the juvenile night to 9 GP tipped `(9/8)^1.15 = 1.145` over a ceiling boundary into **2 LT**. So the discovery year paid **0.222 LT per GP risked against a real card's 0.167** — the cheapest company in the game paying the best land in it, exactly backwards from "fighting up pays extra" (config's `LAND` block, ruled 2026-08-03). Nothing in the engine or the tests could see it: every number was internally consistent, the invariants all passed, and the ruling it broke lives in a comment. It surfaced because a subagent writing the Handbook's land page **refused to assert a direction the arithmetic contradicted** and wrote the page to branch on the measured number instead. `LAND.FEE_PER_TOKEN` 8 → 9 fixes it at the source: a full juvenile night lands exactly on 1.0 and the direction is right again (0.111 against 0.143). The cost, taken deliberately: a full real night 7 LT → **6**, a Major's round 56 → **49**, a ~13% land haircut across the board, in preference to a 100% rise in juvenile land supply. **The branch in `wiki/land/page.tsx` STAYS** — it is the tripwire for the next fee change that trips the same boundary. The floor still bites BELOW a full night (a juvenile that got 2 of 3 risks 6 GP and floors at 1 LT = 0.167/GP, better than a real short card's 0.143); that is the floor doing its job and is not worth curing.
- **A SIXTH INVARIANT: `fight counts match the log`.** `lobbyEntries.fights` stopped being a description and became arithmetic — the refund is `fee - stakePerFight(fee) × fights` — so a miscount hands the wrong number of GP to a real wallet. And it is **invisible to GP conservation**, because paying the wrong party out of a balanced escrow still balances. Before this round it could not go wrong: `battle_log_id` was a POINTER, and a pointer is either right or null; a counter can be quietly off by one. The check reconciles every settled entry against the battle log and refuses any entry over `FIGHTS_PER_GROUP_BIRD`, which also catches a broken deal. This is the same class of bug as round 14's gacha burn and round 22's `buyLand` burn, one layer further in — the conservation proof caught those two and could not have caught this one.
- **RENAME: `CADENCE.FIGHTS_PER_BIRD_PER_DAY` → `ENTRIES_PER_BIRD_PER_DAY`,** and `Lobbies.checkFightCap` → `checkCardCap`. The rule never changed (one card per bird per game-day) but the old name described it correctly only while one entry meant one fight. The gate was rewritten too: it used to SUM today's battle-log rows with pending entries against a cap of 1, which stops meaning anything when a settled card leaves three rows. It now asks two separate questions — is the bird already carded today, and did it fight a bracket today (tournament rows are the ones with a null `lobbyId`).
- **SCHEMA.** `lobby_entries` drops `opponent_entry_id` and `battle_log_id`, gains `group_no` (set at close) and `fights` (set at resolution). `EntryCard.drew` changes from `{bird,farm} | null` to an ARRAY — the bird's group minus itself and minus its own barn-mates, empty meaning it drew nobody. `FightReport.landEach` is gone (land is no longer a property of a fight); `FightReport` gains `groupNo` and `stake`, and `LobbyResolution` gains `settlements` — one line per entry, so a SHORT card is reportable at all. New event type `card_settled`.
- **`Topline.cancelled` now counts entries, not `refund` events.** It always counted championship scratches alongside unmatched birds, which was tolerable while unmatched ran in the hundreds. The group stage takes unmatched to near zero, which would have left a counter labelled "drew nobody" reporting mostly scratches — a number that gets MORE wrong exactly as the thing it names gets better.
- **"NO FIGHT, NO CLAIM" SOFTENED TO "no fight AT ALL, no claim".** A claimer sale goes through if the bird fought at least once. A short card is the lobby's fault, not the bird's, and voiding a sale over it would punish the seller for the draw. The round-23 rule stands for a bird that drew nobody: fee and every claim refund in full.
- **THE RESULT, on a fresh 91-day world: 0 warnings, 0 invariant failures (all six).** Entries that never drew an opponent **5.7% → 0.9%** (71 of 7,670). Fights **~3,700 → 10,725**. Mean fights per settled entry **2.59 of 3**; full cards **65.4%**, short 33.6%, never-fought 0.9%. Groups: 2,187 dealt, mean 3.51 birds, **44 of one — every one of them a lobby with a single entry, i.e. zero levelling failures.** Mean birds per lobby 9.94 → **10.88**; same-barn stranding 41 birds → **24**. Majors **30 run / 0 cancelled at a field of 13.7 → 27 run / 3 cancelled at 17.0**; Juvenile Championship 14.0 → **17.3** (22 run / 2 cancelled) — fields grew because every fight in a group banks qualification points separately, so a sweep is 3 points in one night and campaigning is a few nights rather than a month. Discovery at age 4+ **clear home 52.7% → 82.0%** and true-best-blade carding 29.2% → 34.1%, on triple the evidence — three figures at one blade in a night is the cheapest read on a bird the game has ever offered, and it shows. Broodmare band holds at **75.4%**. `bun test src` **357 → 365 pass / 0 fail**, 17,662 assertions; `bunx tsc --noEmit` and `bun run build` clean; `bun run balance` unchanged at 1 warning / 154 rows / 46 findings. A 91-day sim now takes **~13 minutes** (it was ~4) — three times the fights, three times the wall clock.
- **THE COST, NOT SPUN: a third of entries get a SHORT card.** 33.6% fought once or twice instead of three times, and that is levelling working as designed, not a bug — a group of three is two fights, and levelling produces groups of three whenever the field doesn't divide by four. The stake for the unfought share is refunded in full, so nobody is charged for a fight they didn't get. The alternative (packing to 4+4+1) buys more full cards by stranding a bird again, which is the trade this round explicitly refused. If short cards ever want reducing, the lever is `GROUP.SIZE`, not the levelling rule.
- **THE OTHER NUMBER THAT MOVED THE WRONG WAY: gen-2 mean stat gain +30.0 → +19.8**, while home margin rose +6.9 → **+8.7** and stars +0.4 → +0.1★. This is the round-32 selection-pressure watch item reading again, from the other side: nothing about breeding changed this round, so the likely cause is that tripling the fights tripled the EARNINGS spread and moved which birds the bots choose to breed. Not diagnosed — recorded, because a bloodline number that falls 34% deserves a look before another round stacks on top of it.

**Round 35** (2026-08-07) — **THE FEEDBACK LOOP: a 91-day sim goes 13:01 → ~2:20, and we find out that half of what we have been measuring was noise.** A performance round that turned into a methodology round, which is the more valuable half.
- **THERE WERE NO INDEXES. On any table. Ever.** `grep -c "CREATE INDEX"` returned 0. Every lookup on a non-key column was a full table scan, so the sim got slower as the SQUARE of the world's age. The hot path is the scout: `scoutReport`/`formatRecords` fetch a bird's whole fight history to work out its best blade, and the bots call that for every active bird every day — ~32,000 scans of a `battle_log` ending the run at 21,450 rows. Measured on a real 91-day database, 500 of those lookups took **5.36s unindexed and 0.008s indexed**. Nineteen indexes added, composites ordered so the leading column serves the common single-column query too. Normalized by fights (the runs are not the same world — see below), throughput went **13.7 → 70.4 fights/second, a 5.1× speedup**.
- **ONE TICK IS NOW ONE TRANSACTION**, and the atomicity matters more than the speed. A tick moves money in a dozen places and the conservation proof only holds ACROSS the whole day — escrow leaves a wallet at entry and returns at settle-up, and in between the books genuinely do not balance. A crash in the middle used to leave a world permanently short: that is the "800.00 GP MISSING" shape we saw twice from interrupted sims and both times had to reason past. Plus `synchronous = NORMAL` (safe under WAL — a power cut can cost recent commits but never integrity) and a 64 MB page cache against a 60 MB working set.
- **A QUADRATIC HIDING INSIDE BOOKKEEPING.** `computeTopline` ran `.all()` and filtered in JS to find the handful of PENDING entries, and pulled all 21,000 battle-log rows into JS to measure `.length`. `recordSnapshot` calls it **twice per tick**, so every day paid for the world's entire history. Now filtered and counted in SQLite.
- **⚠ THE FINDING THAT MATTERS: THE SIMULATION WAS NEVER DETERMINISTIC, and it invalidated years of our own measurements.** `freshSeed()` — the one non-deterministic call in the whole engine — returned `Date.now() ^ Math.random()`. Every run built a DIFFERENT WORLD. That quietly broke the two things we use simulation for:
  1. **A/B measurement.** Round 35's own speed runs clocked 2:37, 2:22 and 2:58 while fighting 10,556, 10,000 and 10,277 fights — the wall-clock difference was mostly different amounts of work. The optimizations could not be told from flukes.
  2. **Balance reading, and this is the expensive one.** SIX 91-day runs of effectively identical code produced gen-2 mean stat gains of **+34.4, +23.4, +23.2, +26.0, +17.3, +16.1** — an **18-point spread from nothing but the seed**. Round 34 opened an investigation into a "fall" of 10.2 points (+30.0 → +19.8) and wrote a hypothesis into PROGRESS.md. **There was nothing to find; the delta sat entirely inside the band.** Worse, the hypothesis was independently wrong: `foalScore` reads stats only — shape separation, level, anchors, half-stars — and is blind to earnings, records and fight counts, so the group stage could not have changed which pairs the bots choose by the route proposed.
- **`seedWorld(n)` and `bun run simulate --seed=N`.** The same fix serves both problems because they are the same problem: use ONE seed to A/B two code paths honestly, and a SPREAD of seeds to learn how big a delta has to be before it means anything. Live play is untouched (unseeded behaves exactly as before). This is the sim's `--converge` moment — `bun run balance` has measured its own noise since round 26 and the world simulation never could.
- **WHICH METRICS SURVIVE THE BAND, measured across three seeds at 91 days.** ROCK SOLID: unmatched **1.0% / 1.0% / 1.0%**, full cards **63.1% / 64.8% / 63.0%**, mean lobby fill **10.40 / 10.22 / 10.72**. NOISE: gen-2 mean stat (18-point range), gen-2 home margin (+6.8 / +7.7 / +12.8). **So round 34's group-stage claims replicate exactly and its bloodline worry does not exist** — structural and mechanical numbers are trustworthy to a fraction of a point, selection numbers need replicates before anyone reads a delta.
- **A DDL SPLITTER BUG, found immediately by the new comments.** `splitStatements` split the raw DDL on ";" — so a semicolon inside a `--` comment ended a statement early and left a comment-only chunk that bun:sqlite rejects as an empty query. The index block explains WHY each index exists, per the house comment rule, and one of those sentences contained a semicolon. Comments are stripped before splitting now: given how densely this codebase comments, the DDL had to become commentable rather than the comment become semicolon-free.
- **Still open, approved and NOT done this round:** LT minted in hundredths (the structural cure for round 34's rounding inversion — the guard test for it is also still owed), dropping the persisted `play_by_play` (35 MB of a 59 MB database, and derivable from the stored seed since `simulatePair` is deterministic), and the N+1 reads in the closed-lobby reveal.

**Round 36** (2026-08-07) — **LAND MINTS IN HUNDREDTHS, and the rounding bug class is closed.** Round 34 created an inversion and papered over it; round 35 proved the measurement it was argued from was noise; round 36 fixes the cause.
- **THE BUG, restated as a class rather than an incident.** Round 34 moved an entry fee 8 → 9 GP. The land curve was `max(1, ceil((fee/8)^1.15))` in WHOLE tokens, and `(9/8)^1.15 = 1.145` — `ceil` made that **two tokens**. So the discovery year minted **0.222 LT per GP risked against a real card's 0.167**: the cheapest company in the game paying the best land in it, exactly reversing the standing "fighting up pays extra" ruling. **Whole tokens made `ceil` LOAD-BEARING.** At the cheap end of a superlinear curve the rounding is worth more than the exponent — the same `ceil` is worth 75% on a 9 GP night and 4% on a 42 GP one — so the distortion was always largest exactly where the curve is shallowest. Round 34's answer (move `FEE_PER_TOKEN` 8 → 9 until the numbers landed on friendly integers) worked once, cost a ~13% haircut everywhere, and would have broken again on the next fee change, silently, because nothing could see it.
- **`landForFight` and `landForTournamentFight` now return HUNDREDTHS**, with `LT_CENTS = 100` and `fmtLt(cents)` beside them; `farms.landTokens` → **`landTokensCents`**, `stakedLand` → **`stakedLandCents`**, and `landBoughtToday` / `landGranted` follow. GP has been kept to the cent since the beginning — land was the last currency still rounding to whole units, and it is the one that broke. **`FEE_PER_TOKEN` goes back to 8** and round 34's haircut is handed back: a full real night 6.73 LT (was 7, then 6), a juvenile night 1.15, a Major's round 55.90 (was 56, then 49).
- **THE GUARD TEST THAT SHOULD HAVE EXISTED BEFORE ROUND 34 TOUCHED A FEE.** `LT per GP risked never decreases as the fee rises`, looped over every fee from **1 to 300 GP**. It is the assertion that would have caught the inversion the moment the fee moved. The subagent that wrote it verified the claim numerically first: replaying the old whole-token curve produces **235 inversions at base 8 and 243 at base 9** across that range — so the property is not merely broken by a badly chosen base, it is UNWRITABLE with integer tokens. Under hundredths it passes with zero. A second test pins the ordering the ruling actually names (real above juvenile, Majors above the daily card, and the two curves agreeing at their shared base).
- **⚠ ROUNDING IS AN INTERFACE, AND THE PUBLIC ONE STAYS WHOLE.** `buyLand`, `stake` and `unstake` still take whole tokens: nobody buys 6.73 tokens, and a cap ruled as "1,000 LT a day" must read as 1,000 at every surface a player touches. The conversion happens at exactly one boundary. **The rule of thumb, now written into the Handbook and the MCP prose: you EARN fractional land, you BUY and STAKE round numbers.** Every config knob a human ruled as a round number is written `n * LT_CENTS` for the same reason — spelled as raw hundredths, a later edit "correcting" 10000 back to 100 silently makes a stud seat cost one token.
- **THREE SILENT-FAILURE BUGS THE RENAME CREATED, all the same shape, and this is the round's real lesson.** Every one passed a hundredths figure to an API expecting whole tokens, and every one would have failed INVISIBLY:
  1. `Bots.playFarm` staked its raw balance — a 100× over-request that `stake` refuses and **`quietly()` swallows**. Every bot in the world would have stopped staking, with no error anywhere.
  2. The landlord bot compared `affordable` against `DAILY_BUY_CAP` in mismatched units, so `buyLand` refused it and `quietly()` ate that too. Land purchases were **72,800 GP of the 87,510 GP** the staker pool paid out over 91 days — the entire staking economy would have gone quiet while nothing looked broken.
  3. `PINTAKASI.LAND_GRANTS` and `JUVENILE_MAJOR.LAND_GRANTS` were left as bare integers, so a Major champion would have received **0.05 LT** and a round-of-64 loser 0.70 — a 100× cut to "land to the fallen", the thing that makes losing a hardcore bracket survivable. Found by the subagent writing the Handbook, from the rendered page, not from the code.
- **N+1 READS IN THE LOBBY REVEAL.** `card()` queried for every name it printed — a `farms` row and a fresh `Flock` per entry, plus a `birds` AND a `farms` lookup for every group-mate in the draw. Under the group stage a closed lobby of twenty reveals ~60 group-mate names, so rendering one room cost well over a hundred queries to print twenty rows. Two batched reads now. Worth doing separately from round 35's indexes precisely because indexes turn a catastrophe into a papercut, and a papercut per row is still the wrong shape.
- **A REASONING ERROR OF MINE, corrected rather than left standing.** Round 34's config comment justified paying land once per NIGHT partly on the grounds that per-fight payment would flatten the curve, "since 14 GP and 3 GP both sit near the `max(1, …)` floor". Hundredths dissolved that floor — 14 GP is 1.90 LT now, not a floored 1 — so half the argument stopped being true. Per-night is still right, for a cleaner reason: the curve is superlinear, so `f(a+b+c) > f(a)+f(b)+f(c)`, and a full real night pays **6.73 against the 5.70** three separate 14 GP fights would. That is not a bonus bolted on; it is the same "fighting up pays extra" property read across one bird's evening. The test that asserted the old direction was flipped, with the change recorded.
- **NEW DOCTOR SECTION — `LAND SUPPLY`**: circulating / staked / idle, total minted with a per-day mint rate, and the burn. It exists because round 34 took ~13% off every award and round 36 hands it back, and **both changes were argued from the config file because there was no measurement to argue from.** The arithmetic is exact rather than estimated — the stud seat is the only negative land delta any engine path writes, so `minted = circulating + burned` holds. The audit also confirmed no `DOCTOR` threshold was land-denominated, so nothing silently changed scale; a warning that stops firing is invisible in a way a wrong printed number is not.
- **THE RESULT, on a seeded 91-day world (`--seed=101`): 0 warnings, 0 invariant failures across all six.** Land supply 169,521.72 LT circulating, 93.8% staked, 2,045.89 LT/day minted, 18,700 LT burned into stud seats. Unmatched **1.1%**, full cards 63.0%, fill 10.35 — all inside round 35's measured replication band, so the group stage is unchanged. Majors field 16.0, Juvenile 18.3. Adoption holds everywhere it held (studs listed 14/15, claims 14/15, Major entries 14/15). `bun test src` **365 → 369 pass / 0 fail** (17,970 assertions); `bunx tsc --noEmit` and `bun run build` clean; `bun run balance` unchanged at 1 warning / 154 rows / 46 findings. gen 2 vs gen 0 reads +21.8 mean stat — squarely inside the ±9-point band round 35 measured, and therefore **not** reported as a movement.
- **Left open, deliberately: LT CONSERVATION IS NOT YET PROVABLE.** GP has a two-sided proof (what IS versus what was ALLOWED) and land has none, because the Majors' per-fight mint credits land but records it as `data.landEach` on a `fight` event rather than as a signed `lt` delta per side. So `sum(events.lt)` does not reconcile against wallets — not a bug in the money, a gap in the ledger. Making that mint emit signed deltas would make land conservation provable the way GP is, and is the natural next invariant.

**Round 37** (2026-08-07) — **THURSDAY OPENS, LAND GETS ITS CONSERVATION PROOF, AND TWO LEDGER LIES ARE FIXED.** Three approved items and a charts/grids pass on the office — but the round's real content is that giving land a proof immediately found things, which is the whole argument for invariants over inspection.
- **QUALIFICATION POINTS ARE DELETED, and Thursday is open.** Zane: *"let's just shift to earnings-based and get rid of qualifier points concept."* `PINTAKASI.POINTS_FOR`, `PINTAKASI.QUALIFYING_POINTS` and the `birds.crown_points` column are gone. Any active, named, age-`FORK` bird may declare for a Major; **age is now the only hard gate in the senior game**, and the Selection Committee — which already existed and already ranked — seats the field on **career earnings → wins → average figure → id**. Three reasons this is better rather than merely smaller. (1) Points were a **second scoreboard measuring almost the same thing as earnings** (win real fights) while being visible nowhere except one gate, so the game taught a number whose consequences a player could never watch. (2) A threshold is **binary**: at 3 points you were in, at 2 you were nothing, and the world's 40th-best bird had exactly the standing of its 4th. A rank is continuous — every GP a bird ever earned moves it up the list. (3) `MAX_BRACKET` was always the real ceiling; deleting the gate makes it **the actual contest**, and the committee's existing bump line does the selecting. Registering is no longer standing, and the Handbook and MCP prose now say so in as many words.
- **⚠ THE RESTRAINT HAD TO MOVE, NOT VANISH.** The Majors are hardcore, so an engine that will take an unraced age-3 bird into a bracket that force-retires losers is a barn-culling machine if nothing else says no. The check moved from a RULE to an APPETITE: `CROWN_CHASE.CROWN_MIN_REAL_WINS` (bot-config, currently **1**, on `stakesWins` so juvenile practice doesn't count). Far looser than the 3 real wins it replaces — that is the point of opening Thursday — but not nothing. A test pins the asymmetry: a bird below the bots' bar must still be acceptable to `Tournaments.enter` directly, because that is what makes it appetite and not a second gate.
- **LT CONSERVATION IS THE SEVENTH INVARIANT.** `sum(events.lt) == sum(farms.landTokensCents + stakedLandCents)`. The blocker named at the end of round 36 was exactly right: the Majors minted per fight but recorded the award as `data.landEach` on a world-level `fight` event — unsigned, belonging to no farm — so there was nothing to sum. That mint now writes its own signed per-farm `crown_land` row. Land is a **simpler** thing to prove than GP: no escrows, no pools, so no timing window where the books are legitimately out. A mismatch is always a bug. Staking is deliberately silent (it moves land between a farm's own two piles), which is why the right-hand side sums both.
- **AND IT IMMEDIATELY FOUND A BUG — A REAL ONE, INVISIBLE FOR FOURTEEN ROUNDS.** `Gacha.bundle` passed `silent = (i === BUNDLE_ROLLS - 1)`, silencing only the **last** roll. So a bundle wrote twelve ledger lines instead of the one its own doc comment promised, **and that one silent roll minted a Land Token no `lt` delta ever recorded**. Every gacha bundle ever bought put the world's land books permanently one token out. Nothing could see it: the money was right, the land was right, only the *ledger* was short, and no test asserted a bundle's shape. Fixed at the cause (every roll silent, the summary carries the whole bundle's land in one delta) and pinned by a two-sided guard test — one line per bundle, **and** the farm's land increase equal to the deltas written. Half that test would not have caught it.
- **A SECOND LEDGER LIE, found by reading the sim rather than the code, and this one was poisoning the most important number in the game.** The `retire` emit in `runTournamentFight` sat **outside** its `if (hardcore)` guard, and had since the juvenile division was added in round 23. Every Juvenile Championship loser wrote a `retire` event saying it had been *force-retired* while walking home alive — **67 of them in a 21-day sim against 0 actual retirements.** The bird was fine (the status update WAS guarded); the ledger was fiction. The serious cost is that the doctor's POPULATION block counts `retire` events by `data.by` and **warns when attrition outruns supply** — so the single balance signal we rely on was reading invented deaths, and it would have made this round's open Thursday look like a population collapse that never happened. The `fight` event's "force-retired" narration had the same unguarded shape.
- **FIVE MORE BOT STABLES (15 → 20), chosen to fill HOLES rather than add bodies.** The existing roster covered the middle of every knob and none of the extremes, and an extreme is where a mechanic gets tested. **Batangas Sprint Club** breeds `housePair` 0 hard — sprint blood is deliberately the scarcest in the game, but scarcity is only a price signal if a supply curve exists at all. **Hacienda Verde** is a landlord at `landAppetite` 0.35 against Lupa's 1.0, and **Sugalan Social Club** a whale at `gachaAppetite` 0.3 against Ginto's 1.0 — both markets had exactly ONE customer each, which measures a ceiling and nothing else. **Pulang Bagwis** lives at `tagCourage` 0.05, so the 50 GP claimer rung becomes a market instead of a rung the config mentions. And **Ilonggo Ironworks** is this round's stress test: lowest `entryRate` in the game (0.35), highest crown nerve (0.95) — it campaigns barely and throws everything at Thursday, so if the open field is going to overfill brackets or cull the adult population, it is the barn that shows it. *Measured: both new speculators walked through their doors on day one (Hacienda 27 land buys, Sugalan 196 bundles — more than daily-rolling Ginto's 154, because Ginto rolls itself broke and Sugalan stays liquid enough to keep buying).*
- **THE OFFICE**: three hand-rolled SVG charts (fights/day; breeds/day + cumulative breed spend; claims/day + cumulative claim spend — no charting dependency, `<title>` tooltips, still a server component), AG Grid `fitCellContents` autosizing across every grid, **GP fixed at two decimals everywhere** to match land (`perLt` stays at three, being a rate), the Breeding tab's day column traded for egg sprites, and **click a bird to open its fight history below the flock**, newest first. The favicon is now the actual `ROOSTER` pixel map from `sprites.tsx` rather than a hand-drawn approximation of it — mirrored on X, because every bird in the office faces right.
- **THE RESULT, on a seeded 91-day world (`--seed=101`): 0 warnings, 0 invariant failures across all seven.** Majors **29 run / 0 cancelled at a field of 22.5** (round 36: 16.0) and Juvenile 23 / 0 at 21.6 — opening Thursday grew the fields by half and cancelled nothing. Population healthy and **no attrition warning**: active 444, retired 663, supply 1,312 against 663 losses. Land supply 237,981 LT circulating, 93.4% staked, 26,600 burned into stud seats — and the new by-source split, which is only writable now that the ledger is complete, reads buy_land 44.6% · **crown_land 26.7%** · card_settled 15.8% · purse_payout 11.1% · gacha 1.8%. Adoption 19 of 20 farms on claims, studs, Major entries and the juvenile crown. gen 2 vs gen 0 **+15.1 mean stat** — reported as a number, not a movement, per round 35's noise band.
- **THREE MORE THINGS THE TEST PASS TURNED UP, all pre-existing, two fixed.** (1) **`Tournaments.enter` lied to juvenile registrants.** Its ledger line and its returned `note` both said "hardcore: lose and the career ends" and "runs Thursday — every loser force-retires" **unconditionally**, since round 23. For a juvenile all three claims are false: Wednesday, and the one crown in the game that does not end a career. That `note` is the exact prose an agent reads back to a player before asking them to confirm risking a bird's life, so it is a correctness bug wearing a typo's clothes. Both lines branch on the division charter now, and `DAY_NAMES` moved into config so a rule that says "runs on day 6" can say "Thursday" wherever it is quoted. (2) `committeeCards`' doc comment still advertised "points". (3) **The test suite has been ~83% reliable and nobody knew** — `doctor.test.ts`'s breeding-selection case failed about 1 run in 6 because `freshSeed()` is `Date.now()`-based unless the world stream is seeded, so it built a different 20-day world every run and measured a thin sample. Pinned with `seedWorld(3737)`. Left alone deliberately: `refundEntry` says "Pintakasi entry withdrawn" for a juvenile scratch, which is loose rather than false, and threading a label through two call sites to fix a cosmetic line is not worth the churn.
- **VERIFIED:** `bun test src` **369 → 387 pass / 0 fail** (18,072 assertions, 26 files — a new `land.test.ts` among them); `bunx tsc --noEmit` and `bun run build` clean; `bun run balance` unchanged at **1 warning · 154 rows · 46 findings**.

**Round 38** (2026-08-07) — **THE FIGHT LOG IS REGENERATED, NOT STORED — and the check built to make that safe immediately found a five-round-old archive bug.**
- **`battle_log.play_by_play` IS DELETED.** ~1.8 KB of turn-by-turn narration a row, **51 MB of a 90 MB database**, written 28,710 times a 91-day sim and **read back by nothing, anywhere** — not the office, not the API, not the doctor, not a test. It was pure write amplification with a plausible-sounding name.
- **IT DID NOT NEED STORING, because a fight is a pure function of a seed.** `simulatePair(A, B, format, mulberry32(seed), header, weather)` — every argument survives in the row or is derivable from it. And the combatants survive because of a decision made twenty-odd rounds earlier for entirely different reasons: **STATS ARE FIXED AT BIRTH.** There is no training in this game, so a bird's combat slice today is what it was the night it fought. That is the whole trick, and a game with training could not do it. `src/engine/replay.ts` rebuilds any fight from its battle-log id; the office now serves it at `/api/fight/[id]` and shows it when you click a fight in a bird's history.
- **⚠ THE GUARD WAS THE POINT, not the storage saving.** The obvious objection to deleting a transcript is drift: retune `simulatePair`, `FORMATS`, `BATTLE`, `FIGURE` or the rng and every historical seed replays into a DIFFERENT fight — possibly one the loser wins. A transcript that contradicts the result stored beside it is strictly worse than no transcript. So every replay is checked against what the archive already knows (both Pit Figures and who won are stored per row), and a mismatch is reported rather than shown: `drifted` says "this fight happened, here is its result, but the engine that fought it no longer exists." `null` — unreconstructible — is a different answer and renders differently. **And nothing else in the project would ever notice drift**: a retuned fight engine breaks no test and moves no invariant, it just quietly turns history into fiction. Hence the doctor's new `REPLAY` line, sampling 200 rows spread across the world's history.
- **THE FIRST RUN OF THAT LINE CAME BACK 199/201, AND THE TWO FAILURES WERE A REAL BUG — pre-existing since round 23, nothing to do with this round.** Both were Juvenile Championship fights. `runFight` wrote every battle-log row with `dayIndex: t.weekIndex * 7 + PINTAKASI.DAY_OF_WEEK` **hardcoded**, so a juvenile crown — which runs **Wednesday** — was archived under **Thursday's** date. Two harms, one of which had been running for fifteen rounds. (1) A whole division's fights were dated a day late, and every per-day reading silently inherited it: the office's new fights-per-day chart, `flock.ts`'s form book (which labels a fight's ascendant element with `weatherOfDay(row.dayIndex)`), and any SQL anyone ever wrote. (2) The fight's weather is `weatherOfDay` of the REAL day, so a replay rebuilt from the logged day fought under the wrong element — visible only when it changed the outcome, which is why it read as 1% rather than as everything. Fixed by passing the true `dayIndex` down; the sample now reads **202/202**.
- **DID IT SPEED UP THE SIM? NOT MEANINGFULLY — and saying otherwise would be exactly the round-35 mistake.** Three 91-day runs at `--seed=101` on the same machine: **331.75s with the column, 302.52s without, 325.24s without + the date fix.** The two runs of identical write behaviour differ by 23 seconds, so the 29-second gap against the baseline is *inside* the machine's own noise and cannot be called a speedup from one run each. That is the mechanically sensible answer too: round 35 established the sim is QUERY-bound, and appending 1.8 KB to a WAL page the OS is buffering anyway was never the bottleneck. **The win is disk, and it is enormous: 91.2 MB → 20.4 MB, a 78% cut**, on a file that is copied, opened and scanned constantly during development.
- **VERIFIED:** `bun test src` **387 → 399 pass / 0 fail** (18,161 assertions, 27 files) with a new `replay.test.ts` covering byte-identical rebuild against the live fight, both drift directions independently, `null`-not-a-guess on an unreconstructible fight, the documented rename caveat, and `toCombatant`'s indifference to records/age/farm/status (a bird sold, aged, retired and re-recorded still replays identically). `bunx tsc --noEmit` and `bun run build` clean. Seeded 91-day sim: **0 invariant failures**, all seven.

**Round 39** (2026-08-07) — **WHICH SIDE WAS A IS STORED NOW.** A small round, taken immediately rather than filed, because the thing being fixed was a trap rather than a bug.
- **`battle_log.side` (0 or 1) records which argument each bird was to `simulatePair`.** The two combatants share ONE rng, so the argument order decides who gets which roll: replay a pair swapped and you get a *different fight*, not a mirrored one. Round 38's replay recovered the order by inference — the lower row id was side 0 — which was true only because both engines happen to insert one row per turn of a `for (const [i, side] of sides.entries())` loop. **Nothing enforced that.** Batching those two inserts into a single `.values([a, b])`, or reordering the `sides` array, would have broken every replay in the game *while compiling clean and passing every test*. The drift guard would have caught it and then blamed the fight engine: a true alarm with the wrong cause on it, which is the expensive kind of alarm.
- **⚠ THE COLUMN IS `NOT NULL` WITH NO DEFAULT, and that is the load-bearing decision.** A default would let a future insert site skip the field and silently write a plausible lie — the exact failure the change exists to prevent, reintroduced through the back door. With no default, an insert that forgets a side does not compile. It paid immediately: `tsc` named all six other insert sites (five test fixtures and the two engines) rather than leaving them to be found by a wrong replay months later. Same argument as the "numbers are imported, never typed" rule, applied to a schema.
- **`replayFight` reads the column and no longer sorts by id.** A pair that does not hold exactly one side 0 and one side 1 is **corrupt, not drifted** — there is no order to replay in, so the honest answer is `null` (unavailable). Falling back to the ids there would be the old assumption sneaking back in through the error path.
- **Three tests pin it**, one of which deliberately asserts the *old* insertion-order property still holds — so the two agree today, and the day they stop agreeing is a day nothing breaks. The other two prove the column is actually authoritative: relabel the sides without touching an id and the archived figures come back in the other order (under the old inference this changed nothing at all), and a pair claiming the same side twice is unavailable rather than guessed.
- **VERIFIED:** `bun test src` **399 → 403 pass / 0 fail** (18,267 assertions, 27 files); `bunx tsc --noEmit` clean; world remade and reseeded per the schema rule. Seeded 91-day sim (`--seed=101`): **0 warnings, 0 invariant failures** across all seven, `REPLAY 202/202`. Majors 28 run / 2 cancelled at a field of 23.4, juvenile 24 / 0 at 20.6, adoption 19 of 20 farms on every door — all inside the round-37 band, as a pure-plumbing round should be.

**Round 40** (2026-08-07) — **EVERY WIN PAYS, THE BOTS STOP LEAVING LAND ON THE TABLE, AND THE BRACKET LEARNS TO SHOW A CAREER.** Four asks from Zane, and the two engine ones each turned up something the report could not previously see.
- **THE CHAMPIONSHIP PURSE IS PAID ON FIGHTS WON, NOT ON FINISHING STAGE.** Zane: *"I noticed that first round winners receive no GP at all. Every round should pay the winners something, even if the winners get to continue on towards championship contention."* He was exactly right, and the number was worse than it sounds: the old table paid champion 0.5 / runner-up 0.2 / each semifinalist 0.1 / each quarterfinalist 0.025 and nothing below, so **a 32-bird Major paid 8 birds out of 31**. A bird could win the hardest fight in the game — hardcore, its own career on the table — and take home zero. `PURSE_SHARES` is replaced by `PURSE = { ADVANCEMENT, CHAMPION, RUNNER_UP }`: **ADVANCEMENT splits across every fight WON, with a win in each round worth double a win in the round before**, and the two placement bonuses ride on top. The doubling is the design — each round hands out the same total across half as many birds, so the curve stays steep without anybody being paid nothing. Majors 0.5/0.35/0.15, juveniles 0.65/0.2/0.15 (flatter on purpose: the discovery year is buying the behaviour of showing up with a live one and winning a fight). Measured on the seeded 91-day world: **paid 334 of 654 Major entrants, 51%** — which is every bird that won a fight, since half a bracket loses in round one.
- **TWO RULINGS SURVIVED THE REWRITE AS ARITHMETIC RATHER THAN AS CLAUSES.** (1) *First-round losers take nothing* was an explicit `round > 1` clause bolted onto the share table; now a bird with no wins simply has no weight. (2) ⚠ **A BYE IS NOT A WIN** — byes exist because the field was short, and weight accrues only where a fight was fought, tracked as the bracket runs. And one deliberate coupling: **the placement bonuses also require a win**, because in a STRAIGHT FINAL the runner-up is also a first-round loser, and paying it for losing the only fight of the night would have quietly reversed a round-18 ruling nobody asked to reverse. The existing test caught that within a minute of the first run.
- **THE DOCTOR NOW PRINTS WHO GOT PAID, not just what the purse was.** The old CHAMPIONSHIPS block printed field size and purse total — so *"the money is there"* and *"the money reaches the winners"* read as the same sentence, and the entire defect above was invisible to the report that exists to find defects. It now prints **paid N/M entrants, the biggest take as a share of all purse GP, and the smallest take**.
- **THE BOTS WERE ALREADY STAKING EVERYTHING — THEY WERE JUST A DAY LATE.** Zane: *"stables have a lot of LT sitting unstaked. They should be immediately staking everything they get."* The diagnosis was not a missing appetite, it was a missing hour: a bot's day runs BEFORE the card goes off, and land is minted when fights settle, so every barn's idle balance was **exactly one night's winnings** — checked farm by farm, to the token (Pulang Bagwis idle 1,804.04 against 1,803.21 earned that day). `Bots.sweepStakes` banks it the moment the card ends. ⚠ It runs AFTER `distributeStaking`, on purpose: sweeping first would let tonight's land draw tonight's payout, a better deal than any player gets. Idle land **16,224 LT (6.7%) → 1,301 LT (0.5%)**, and what is left is the one non-bot farm.
- **⚠ AND THE SWEEP IMMEDIATELY BROKE THE STUD MARKET, IN EXACTLY THE SHAPE AGENTS.md WARNS ABOUT.** The next sim read **studs listed 4 of 20**, down from 19. A stud seat costs 100 LT *liquid*, and the bots' day was ordered to list studs before staking precisely so they would have it — but the overnight sweep meant they now woke up with nothing, and `quietly` ate twenty refusals a day with no error anywhere. **Nothing but the adoption block would have caught it**; every invariant passed, every test passed, and the feature was simply gone. The fix is the ordering Zane described in the first place: stake everything, and the one thing worth pulling land back out for is a stud seat — the bots now **unstake exactly the shortfall**, only when a rooster is standing there waiting for it. Back to **20 of 20**.
- **⚠ THE PROMISE HAD A TRAPDOOR, AND THE ROUND'S OWN TEST PASS FOUND IT — nothing failed.** A shallow win in a big bracket is a tiny FRACTION, and `Math.floor` of a tiny fraction of a thin purse is zero, at which point `payPurse` skips the row and the bird is back to going home with nothing. Measured: a 64-bracket paying out of a 3.00 GP purse left **16 of its 32 fight-winners unpaid**. Nothing anywhere would have said so — the purse still settles to the cent because the champion absorbs the remainder, so the conservation proof stays green while the new rule quietly stops being true. **A cent is the floor now**, with an honest fallback: if the purse cannot cover a cent for every winner and one for the champion, the floor stands down and the deepest winners take what there is, because the alternative is printing GP. The break-even is 3.84 GP for a 64-bracket and 1.60 for a 32 — a real Major purse is a thousand times that, which is exactly why this would have sat there for twenty rounds.
- **LEFT AS RULED, AND PINNED SO IT ISN'T "FIXED" LATER:** the committee's top seed can take a first-round bye, reach the final, lose it and take home **no GP at all** (it keeps its land grant), while a bird it never met gets paid for winning one fight in the other half. That reads sharp, and it is round 18's ruling holding: a bye is not a win, and the placement bonus does not pay a bird that never won. Both legs are now covered by a deterministic fixture.
- **THE PAYOUT ARITHMETIC LIVES IN ONE PLACE.** Within an hour of the rule landing, the Handbook and the MCP tool descriptions had each grown their own copy of the closed form. `config.purseShareOf` is now the single export both read — a deliberate SECOND implementation, since the engine has to count weight as the fights happen (the only place a bye can be told from a win) while a page describing the general case wants a full bracket. Two implementations a test compares is a guard; three that nothing compares is how documentation starts lying. `docs.test.ts` pins the closed form against the rule for every bracket size from 4 to 64 — including the one bracket that deliberately does NOT sum to 1 (a straight final, where the unallocated runner-up share renormalizes onto the champion).
- **THE BRACKET SHOWS A CAREER NOW.** Each fighter carries a third line: **record and NET career earnings, current as of that fight** (net, so it can read negative, and it does — that is honest). The champion-only column is gone; the 🏆 sits on the winner of the Final, which is where it always belonged. Vertical spacing tightened — and the interesting part is where the air actually was: a round-one card was ~72px in a 104px slot, so the fix was the *line heights*, not the seat unit, which moved only 3.25rem → 3.2rem. The limiting case (a round-one card with two real fighters, at the narrowest column width) was measured in a headless browser rather than guessed, and came out with 3.4px to spare. A latent bug fell out on the way: `.bfarm` had `text-overflow: ellipsis` with no `white-space: nowrap`, so a long farm name wrapped and would have clipped every round-one card the moment a third line existed.

**Round 41** (2026-08-07) — **THE MAJORS COST 80 GP AGAIN, AND EVERY WIN STILL PAYS FOR ITSELF.** Round 40 made the purse pay on fights won; Zane asked the next question — should entry still be free? — and the ledger answered it.
- **WHO WAS ACTUALLY PAYING FOR THE CROWNS.** The purse is the juice pool's, and traced over a 91-day world that pool came from **gacha spend 187,056 GP (57%), breed fees 136,040 GP (42%), the genesis seed 2,400 (1%) — and the entrants 0%.** Only 2 of 20 barns buy gacha bundles. So the biggest stage in the game was bankrolled by whoever happened to be rolling the gacha, and a barn that never entered a Major still paid for its purse through every cover it bought. `PINTAKASI.ENTRY_FEE` goes **0 → 80** (2× a night's card), reversing round 22 for the price while keeping round 22's actual point: the GATE is still earned, not bought — age is the only hard door and the Committee seats on earnings. Fees add on top of the juice, take no staker rake, and are escrowed per entry, so a reprice refunds what was actually paid.
- **⚠ ONE KNOB WAS CHARGING BOTH DIVISIONS.** `findOrOpen` stamped `PINTAKASI.ENTRY_FEE` on **every** tournament row in the game, juveniles included — invisible while it was 0, and an 80 GP toll on age-1 chicks the moment it wasn't. `DIVISION_RULES` carries `entryFee` now and `JUVENILE_MAJOR.ENTRY_FEE` is its own 0. **The Juvenile Championship stays free**, deliberately: the discovery year exists to find out what a bird is, and it is the one crown that does not end a career.
- **THE ROUND MULTIPLIER IS THE KNOB THAT MOVES THE MEDIAN — the three shares can't.** A round-`r` win scored `2^(r-1)`, and against an 80 GP door that put a first-round win in a 32-bird Major at **53 GP — net −27**, which is round 40's complaint wearing a different hat. Re-weighting the shares does not reach it (ADVANCEMENT 0.60 / CHAMPION 0.28 at ×2 still gives −19; closing it that way needs ADVANCEMENT near 0.95, which guts the trophy). `ROUND_MULTIPLIER` **2 → 1.5** does, cheaply. Net GP after the fee, 32-bird bracket, 5,931 of juice: champion **+4,039**, runner-up +1,900, 3 wins +333, 2 wins +137, **1 win +7** — every winner clears the door, and the champion takes **25% more than it did on a free entry** (+3,225) because the fees grow the pot. Zane: *"Champion should still receive a lot, but I suspect there's a happier median."*
- **MEASURED, and the promise holds exactly where it was ruled to.** Seeded 91-day world: in **32-bird brackets, 129 winners and 0 net-negative** (smallest take 82.17 GP against the 80 GP fee). In 64-bird brackets, **7 of 176 winners** came up short — twice the birds sharing one pot, flagged in config as accepted rather than solved, since fields average ~23 and 64 is the busy-week exception. Purse **197,756 → 256,515 GP (+30%)**, of which entry fees now fund **20%**. Fields did **not** shrink (23.3 against 21.7 — reported as "did not shrink", not as growth, per round 35's noise band), 19 of 20 farms still enter, and the juvenile division is untouched at 24 crowns and a field of 21.
- **⚠ THREE DOORS A FEE WOULD HAVE CLOSED WITHOUT A WORD.** (1) `chaseCrowns`'s wallet check **had never executed** — it short-circuited on `ENTRY_FEE > 0`, so it was written for a paid season that hadn't arrived; worse, it `return`ed on an empty wallet and both passes bailed, so a barn that couldn't afford its THIRD blade would have abandoned the two it could. It skips per crown now. (2) `chaseJuvenileCrowns` checked no wallet at all on the stated grounds that the stage "costs nothing" — true, but the fee came off the Majors' knob, so every juvenile entry in the game would have thrown into a bare `catch`. (3) `auto-play` passed **no reserve** to `chaseCrowns`, so an honest stable would have bought championship seats with the money it needed to card its ordinary birds — and crowns run before the daily card.
- **THE BRACKET'S LAND COLUMN WAS TELLING THE OPPOSITE OF THE TRUTH.** Zane asked whether round-winning birds should get "a little LT too". They already do, and generously — `runTournamentFight` mints 55.90 LT to **both** fighters on **every** fight, on top of the elimination grant, so a champion banks **284.5 LT against a first-round loser's 110.9**. The office showed only the grant, which is deliberately inverted ("land to the fallen"), so the column read *champion 5 LT, first-round loser 55*. Across the sim the per-fight mint is **2.4× the grants**. No new land was needed; the display was wrong.
- **THE DOCTOR PRINTS WHO FUNDS THE PURSE**, not just its size — the split is the argument this round is built on, and nothing anywhere showed it.
